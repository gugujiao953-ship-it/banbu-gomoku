# 半步五子棋打谱 v1.1.8

这是相对于 v1.1.7 的更新，聚焦图片识谱的准确性与速度、AI 引擎与分析的实战体验、移动端的导入与存储，并首次把网页版真正做成可以在 iPhone / iPad 上「添加到主屏幕」当本地应用使用的形态。

## 相比 v1.1.7 的主要更新

### 识谱准确性与速度

- **新增「识谱多核加速」并默认开启**（设置 →「可选增强功能」）：按设备的处理器核心数与可用内存自动开多个线程并行识别，把网格探测与逐格分析分摊到多核；生产包真实 UI 全流程实测从 6563ms 降到 2971ms（约 2.2 倍，12 核桌面），识别结果与单线程逐位相同（47 张语料逐位对照），遇到老设备或无法开线程的环境会自动回落单线程，关掉开关也能回到旧行为。
- **修复手机上「白子变黑子」**：印谱里空心白子的前景色统计到的是那圈深色描边，柔化或换设备后这项比例越过阈值，聚类输入被切成最暗四分位，白子就被判进了黑簇。修复后同一张真机谱图在 10 个柔化/变暗变体下颜色全部稳定（改前 1px 柔化即翻色），同时保证实心黑子不会被反向刷白。
- **导入进度会显示图片解码后的像素尺寸**，形如 `… · 89KB · 931×931`，方便一眼看出「同一张图在手机与电脑上结果不同」是不是因为传输时被重压或缩小了。
- **「复原手序」的序号识别继续增强**：采用渲染式识别并支持跨设备字体自适应，他机截图的序号复原能力明显提升，识别失败的图会诚实回退而不是给出错误手序。
- **修复识谱或 SGF 静态局面导入后「分析看的还是空盘」**：设置子现在会正确送进引擎，缺行棋方标记的局面也会按黑白子数推定先后手。

### AI 引擎与分析

- **强力引擎改为应用内下载官方冠军网络（NNUE）**：默认随包轻量引擎，在设置里一键下载约 40MB 的冠军网络，下载完成后下一次 AI 请求即生效，无需重启，也可删除恢复轻量；本版修正了此前的包格式问题，冠军网络自此才真正参与评估。
- **内置开局库（打点簿）**：内置索索夫-8 与山口五手两打两本打点簿，会按开局规则自动选用；命中开局线时 AI 按最强打点毫秒落子，未命中照常思考。人机对战弹窗和设置里都能单独开关，默认关闭。
- **AI 宣布五手打点数会随局面自适应**（不再固定 3 打）：开局越厚，宣布的点数越多，更接近正式比赛对「宣布方制衡黑棋」的要求。
- **分析面板升级为「持续 / 自对弈 / 提示」三模式**：持续分析越算越深、数字持续更新；自对弈可设每步时限、自动对下到五连；提示模式点一下直接为当前行棋方落一手，遇到杀棋立即落子。
- **引擎档位更细**：轻量 / 强力 / 自调三档，自调档可用滑杆在 128–2048MB 之间自由设定分析内存，高内存换取长分析下更深的搜索；分配失败会先降档重试再回落轻量并明确提示。
- **分析与对局更稳更快**：换手和落子后候选点几乎立刻出现、深度和节点只涨不跌、思考指示不再闪烁；点「停止思考」会先落 AI 当前算到的最强点，而不是掐掉回合空等。
- **分析面板更精简好读**：胜率统一标注为「黑方胜率」，候选点按行棋方视角显示并可点击展开该变化的最深预览图。面板小字提醒「本地棋力有限，胜率仅供参考」。

### 网页版与 iOS

- **可以直接在 iPhone / iPad 上把网页添加到主屏幕，当本地应用使用**：用 Safari 打开网页版 → 分享 → 添加到主屏幕，之后会以独立的全屏窗口启动（没有浏览器地址栏），首次联网打开过一次后即可离线使用。
- **补齐了 iOS 专属支持**：新增 `apple-mobile-web-app-capable`、状态栏样式、主屏应用名，以及三种尺寸的 `apple-touch-icon` PNG（由真实 Android 启动图标缩放并压平底色生成，避免 iOS 把透明区域当黑底），新图标会被 iOS 自己再做圆角遮罩。
- **网页版的 AI 引擎包需要应用内下载**：网页版本身不含引擎包，首次需要强力 AI 时会在应用内下载并缓存到浏览器；iOS Safari 对后台下载有回收策略，建议插电、连 Wi‑Fi 后一次性下完，详细步骤见 `docs/iOS-网页版说明.md`。

### 体验与界面

- **功能区布局编辑器**：打谱 / 读谱 / 做题的按钮可以自由拖拽摆放，内置「默认 / 简略」预设模板，每个功能区可一键整区隐藏；走棋导航拆成七个可独立摆放的按钮。
- **标注交互重做**：点已有标注一步取消（不再叠加覆盖），新增「去标注」擦除模式和「把本机标注写入棋谱」一键合入草稿。
- **做题体验重整**：做题里的「分析」永远是提示模式、算到杀棋立刻落子；应战时可以随时换边，重启题目会清空之前所有作答痕迹。
- **快捷中心支持自定义**：右上角「自定义」可勾选显示哪些条目，条目池扩展到覆盖棋盘显示、无障碍、数据与手册等更多设置，且「快捷中心有的设置，设置页里都有」。
- **打谱顶栏更紧凑**：打谱 / 读谱 / 做题三种模式切换移到顶部，退出类按钮收进状态栏右侧；主题列表改为一屏两列的卡片式；设置主目录图标加主题色并去重。
- **使用手册扩充到 19 章并配真实截图**，每章都有一步到位的「引导」按钮，把用户直接带到真实功能位置；首次进入还有 spotlight 式的新手导览。
- **每次版本更新后，首运行页会重新出现一次**：以前「看过就不再出现」的记录改成按版本号保存，升级到新版本时会再看到一次欢迎页（可以「先自己试试」，也可以「带我看看怎么用」进入新手导览）；关掉后写入当前版本号，同一版本内不再打扰。从 v1.1.7 及更早版本升级上来的用户同样会看到这一次。

### 导入导出、存储与更新

- **新增导出 RenLib `.lib` 与 PSQ**：SGF / JSON / LIB 之间互转会保留变化树与注释，PSQ / POS 只保留主线；原格式文件也能直接原样导出。
- **新增「导出并分享」**：把当前棋谱作为 SGF 文件直接交给系统分享面板，微信 / QQ 发出去对方打开即可打谱（对齐「导出即分享」的习惯）。
- **安卓导出位置更好找**：默认写入手机「下载」夹（设备不支持时退回「文档 / 半步五子棋打谱 / 导出」），也可以用系统文件夹选择器指定任意文件夹；设置「文件与存储」会显示当前占用空间。
- **安卓选图默认打开系统相册**：图片识谱点导入后直接停在相册，也可以在系统界面里切换到其他文件夹，不必再从「最近」一层层翻。
- **修复「检查更新」在手机上总是提示失败**：改为站点版本文件 → jsDelivr 镜像 → GitHub 兜底的多源链，并兼容四段构建号，正式包也能正确判断是否有新版本。

### 工程与发布

- 引擎包升级为 v3 官方布局（官方 `.bin.lz4` 数据，NNUE 冠军网络首次真正加载），页面 loader 必须与数据同版本成对发布；装过旧强力包的手机升级后会自动清理旧包并需重新下载。
- Android 的 WebView 缓存迁移键现在随版本号派生（本版为 `v118`），并有单测守住「改版本必须同步改 key」，避免新前端被旧缓存盖住。
- 多线程识别资产随引擎包一并下载，引擎包改为分片并发下载，构建产物不再把大型引擎包塞进 Service Worker 预缓存。
- 新增 3 条识谱回归门禁（多核结果逐位一致、空心白子颜色稳定、输入质量边界）与真机样本 fixture，防止白子翻色、多核不一致这类问题复发。

### 已知问题 / 未验证

- **序号匹配（复原手序）没有并行化**：它需要按手数顺序处理，语义上顺序相关，目前仍由主线程串行执行。
- **识谱多核加速的实际倍数只在 12 核桌面实测**（约 2.2 倍），手机上的真实提升尚未在真机验证。
- **iOS 未在真机验证**：构建链的兼容下限没有实测，建议 iOS 15 及以上；iOS 长时间不访问站点时可能清理站点数据，导致引擎包和本地记录丢失。
- **重度压缩或过小的图片仍可能掉子**：实测同一张谱图原图和 600px 缩放仍能全对，重度重压（如 q0.6）会丢序号、缩到 500px 以下开始出错、400px 可能掉 6–12 子，属已知质量边界。

## Android 安装包

- 包名：`cn.renjunote.mobile`
- 版本名：`1.1.8`
- versionCode：`27`
- 文件与校验：`banbu-gomoku-1.1.8-android-release.apk`（44.61 MB，含冠军网络引擎包 38.44 MB），SHA-256 `FEC46EFFA0B143828DEDEF3745F9078BD5BC2FC1010DF0CD8B1C7FE5D3486EC1`
- 网页版（iPhone / iPad 用 Safari 打开后「分享 → 添加到主屏幕」即可离线使用）：<https://gugujiao953-ship-it.github.io/banbu-gomoku/app/>
- 使用与 v1.1.5、v1.1.6、v1.1.7 相同的 release 签名，可覆盖安装升级。
- **升级提示**：本版引擎包 loader 为 v3（官方 .bin.lz4 布局），装过旧强力包的手机升级后应用会自动清理旧包并需重新下载。

---

# 半步五子棋打谱 v1.1.7

这是相对于 v1.1.6 的更新，聚焦移动端返回键体验、分支树与读谱重命名、新手引导可见性，以及使用手册的全面同步。

## 相比 v1.1.6 的主要更新

### Android 返回键与弹层体验

- 系统返回键现在按层级工作：先关闭当前弹层、工具坞或快捷中心；没有可关闭层时若不在首页则先回首页；在根页面需要连续两次返回才退出应用，并给出二次确认提示，避免误触退出丢失编辑。
- 新增覆盖层历史栈（`useOverlayHistory`）与根层哨兵（`useRootBackExit`），底部弹层与网页端浏览器返回行为一致，并纳入回归 `back-navigation`。

### 分支树与读谱

- 分支树的重命名输入框适配全部主题（含青花瓷、像素、赛博、黑金等深色与彩色主题），不再出现文字与底色对比失衡。
- 读谱模式支持重命名分支：仅作为本机名称覆盖保存，不修改原棋谱，方便给大型定式谱里的分支加自己的标签。

### 新手引导与可见性

- 精简首次进入的引导弹窗，移除“AI 与研究”条目，避免主按钮在窄屏被挤到底部之外。
- 修复新手可见性问题：注释按钮的激活态、以及内置局面数据库入口的展示。

### 使用手册全面同步

- 使用手册扩充到 19 节，覆盖 v1.1.6 之后已实装但此前未写全的能力：VCF 出题训练、山口与索索夫-8 共十项开局规则、AI 思考的胜率/Top-3/主变化、棋谱内查找与跨谱查找、做题摆题与应战、棋谱库手动排序、POS 与 ZIP 导出备份、外接键盘与快捷中心等。
- 手册为各功能点配上真实界面局部截图（约 30 张）并支持点击放大；截图经逐张核验，消除重复图、误拍的未保存草稿弹窗与遮挡，裁切到对应控件区域。

### 工程与发布

- CI 的 Android Release 现在直接产出经 `apksigner` 校验的签名 APK artifact（复用现有 release 证书 secrets），并校验包名、versionName 一致性与 SHA-256。
- 回归脚本对齐现行 UI：多个用例补充首运行引导跳过，导航按钮选择器适配读谱紧凑布局后的新结构。

## Android 安装包

- 包名：`cn.renjunote.mobile`
- 版本名：`1.1.7`
- versionCode：`10`
- 使用与 v1.1.5、v1.1.6 相同的 release 签名，可覆盖安装升级。

---

# 半步五子棋打谱 v1.1.6

这是相对于 v1.1.5 的正式更新。v1.1.5 主要修复了 Android 动效默认关闭问题；本版在保留该修复的基础上，继续扩大旧设备兼容范围，并集中完善移动端打谱、做题、资料管理和内置内容。

## 相比 v1.1.5 的主要更新

### Android 兼容与升级安全

- 修复 Redmi K20 等 Chromium WebView 83 设备因现代 JavaScript 语法导致的白屏，Android 同时提供现代构建与 legacy 兼容构建。
- 移除运行时对 `structuredClone` 的硬依赖，并对 Rapfi 产物处理 `&&=`、`||=`、`??=` 等旧 WebView 无法解析的语法。
- 首次启动时只清理旧 Service Worker 缓存，不删除棋谱、草稿、设置和 IndexedDB；Android 安装包不再注册 PWA Service Worker，网页版仍保留离线 PWA。
- 兼容目标为 Android 8 及以上，继续使用与 v1.1.5 相同的 release 签名，可覆盖安装升级。

### 移动端体验与操作

- 分支树支持手机单指拖动平移、双指缩放和快捷缩放；从节点上开始拖动也可以移动画布，轻点节点仍然用于选择。
- 做题选择器改为“文件夹 → 题库 → 题目”的分层展开方式，题目很多时不会一次列出所有题目，并支持搜索文件夹、题库、题号和题面。
- 新手引导重新整理为更易理解的功能入口，使用手册按功能分行说明，补充导入、图片识谱、主题、棋盘棋子、AI 和资料安全等介绍。
- “关于半步五子棋打谱”增加 QQ 群 `1091866163`；新手引导也提供交流入口，使用手册不放置 QQ 群信息。
- 字号设置统一作用于全局界面文字，同时不整体放大棋盘；棋谱树最小缩放扩展到 30%，增加 30%、50%、100% 等快捷比例。

### 内置题库与棋谱

- 新增 17 个内置题库，共约 6,367 道题，启动后直接出现在“内置题库”文件夹。
- 新增“祁观vs弈心 人机大战五局”内置棋谱文件夹，包含按第 1 局至第 5 局命名的五盘棋谱。
- 新增“九天指南v5-1”内置 DP/DB 局面数据库入口，按需查询，不把大型数据库强行展开成普通棋谱树。
- 内置 SGF、DB、题库 JSON、WASM 和 data 资源进入 Android/PWA 构建产物，并纳入离线预缓存范围。

### GLM2 风险修复

- 补齐设置页 `gold`、`diamond` 棋子选项。
- AI 对局不再悄悄修改全局禁手辅助偏好。
- 保存非 15 路局面为题目时保留棋盘路数和超出第 15 列的坐标。
- 扩展旧 WebView CSS 降级层，覆盖状态栏、选择器、标注卡片和禁用态等关键区域。

## 验证

- 全量 Vitest：62 个测试文件、315 项测试通过。
- Web/PWA 生产构建：通过，资源体积约 7.97MB，低于 20MB 门禁。
- Android Capacitor 同步：通过。
- Android Release 构建：通过。
- APK 签名、包名、`versionCode`、`versionName` 和 SHA-256：通过。
- 本次重新构建包尚未在新的真机安装轮次中复验；历史 v1.1.6 真机证据不能替代本次 APK 的新安装验证。

## Android 安装包

- 包名：`cn.renjunote.mobile`
- 版本名：`1.1.6`
- versionCode：`8`
- 使用与 v1.1.5 相同的 release 签名，可覆盖安装升级。

---

# 半步五子棋打谱 v1.1.5

本版修复 Android 端动效默认关闭的问题。新安装和从 v1.1.4 升级的设备会默认恢复界面动效，用户仍可在“设置 → 棋盘显示 → 界面动效”中手动关闭。

修复范围包括落子、导航、胜线、人机思考提示、主题动态背景、导入解析进度和界面过渡等共用动效链路。

## Android 安装包

- 包名：`cn.renjunote.mobile`
- 版本名：`1.1.5`
- versionCode：`7`
- 使用与 v1.1.4 相同的 release 签名，可覆盖安装升级。

---

# 半步五子棋打谱 v1.1.4

本版以当前主项目状态为准，继续完善移动端打谱、做题、大型棋谱兼容、视觉反馈与发布流程。网页版、PWA 和 Android 使用同一套 React/TypeScript 核心。

## 主要更新

- 保存棋谱弹窗新增可折叠的棋谱信息编辑区，可修改棋手、赛事、规则、日期和开局规则。
- 棋谱库与题库共用搜索入口，支持标题、棋手、主题、题集、来源、题面和先手筛选。
- 支持本地 Rapfi AI、VCF 强制胜搜索、连珠禁手辅助、五手两打/多打、塔十和塔拉规则。
- 完善 SGF/RENJU JSON、LIB、DP、DB 导入与分支浏览，真实大文件使用 Worker、WASM 和紧凑索引处理。
- 增加一键备份与恢复、图片识谱、棋盘图片分享、回收站、默认导出文件夹和批量导出能力。
- 设置页重新按使用顺序折叠分组，包含多套主题、棋盘/棋子皮肤、音效、动效、无障碍字号、可选手势和设备布局；版本标号固定在最底部。
- 优化棋子落点、最后一步、胜线、非法落子、AI 状态、Toast、Bottom Sheet、safe-area 和 reduced-motion 体验。
- QA 正式回归集已覆盖普通棋谱、真实题库、DP/DB、RenLib 大文件、导入导出、AI、视觉与音效路径。

## Android 安装包

- 包名：`cn.renjunote.mobile`
- 版本名：`1.1.4`
- versionCode：`6`
- 使用与 v1.1.3 相同的 release 签名，可覆盖安装升级。
- APK SHA-256：`A75794F70A15625F7D6705A441D2848F979E950C9EEF209EA9580AAAFFC33042`
- APK 使用 Android Signature Scheme v2 签名，下载 `.sha256` 文件可校验完整性。
- Windows 本地后续发布统一执行 `npm run android:release`，流程会自动检查版本、签名、包名和元数据。

---

# 半步五子棋打谱 v1.1.2

本版优化注释查看体验：有注释的局面会显示蓝色提示，展开后的注释框固定高度并支持内部滚动，同时保留底部操作区。

## Android 安装包

- 包名：`cn.renjunote.mobile`
- 版本名：`1.1.2`
- versionCode：`4`

---

# 半步五子棋打谱 v1.1.1

本版修复 DP/DB 网页导入失败后的后台假死提示，并重新发布网页版构建产物。

本版是 1.0.0 之后的首个功能更新。

## 主要更新

- 支持 `.db` / `.dp` LZ4 五子棋数据库导入，已用“九天指南v5-1.db”真实验收。
- 支持 5–25 路动态棋盘及非 15 路 SGF 导入。
- 增强 SGF：设置局面、过手、多顶层棋树、UTF-16 与可验证 JSON 落子列表。
- 加入棋盘图片识谱 MVP，并改进带胜率分析点截图的识别与错误提示。
- 完善手机端打谱：草稿保存、删除变化、黑白棋锁定/自动换色、注释和常驻操作栏。
- 改进大型 LIB 导入、紧凑索引、持久化、跨谱查找和分支显示。

## Android 安装包

- 包名：`cn.renjunote.mobile`
- 版本名：`1.1.1`
- versionCode：`3`
- APK 为 Android debug keystore 签名，可直接侧载安装。
- 同时提供 `.sha256` 文件用于校验下载完整性。

## 已知边界

DP/DB 是私有格式；当前兼容已经确认的 LZ4 + `@BTXT@` 样本结构，其他软件的变体若结构不同会明确报错。图片识谱目前可靠目标是恢复局面；混有胜率数字的截图不会冒险伪造成完整走子顺序。
