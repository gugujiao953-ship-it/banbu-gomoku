# 产品取舍与后续路线

## 调研后保留的桌面端能力

成熟五子棋工具最有价值的部分，是把多个对局或研究分支组织为位置树，并允许逐手前进后退、写注释、做标记、检索与转换格式。手机端首版因此以“棋盘 + 变化树 + 注释 + 格式交换”为核心，没有照搬桌面端密集菜单；棋谱库已提供棋谱名、棋手、赛事主题的即时检索。

本轮对 `D:\五子棋\爱五子棋打谱2.2.3.exe` 做了只读拆解：它还包含坐标/编号显示、Navigate Branch、Find Move、Find Comment or Board Text、Insert/Change Moves、Flip & Rotate、VCF/VCT/UCT、节点文字与多种导出。适合手机的能力已优先迁移为四边坐标、分支后续预览、分叉支点按钮、当前棋谱查找、旋转/镜像、长按标记、改着、A–E 候选点标签、局面文字/着法评价、批量导入去重和可解释的局面候选排序；完整证明型求解器仍属于下一轮专业能力。

## 手机交互原则

- 棋盘始终是第一视觉焦点，关键导航位于底部拇指区。
- 空点轻触落子；已有棋子轻触回看该手；长按用于低频标记。
- 变化、注释、元数据使用底部抽屉，避免离开当前局面。
- 所有主要点击区域不小于 44px；支持 320px 起的窄屏和 Android 安全区。
- 自动保存优先，导出是备份与跨端交换，不要求用户理解数据库概念。

## 三端数据路线

1. 当前：浏览器 localStorage + `.renju` 完整文件，Android / PWA 共用。
2. 下一阶段：桌面壳和网页版账户体系复用 `GameDocument`；大型 LIB 已使用 IndexedDB 紧凑索引，后续加入账户与增量同步。
3. 云同步：文档版本号、节点稳定 ID、软删除与冲突合并；离线创建的变化可在联网后合并。
4. 专业能力：继续完善 RenLib `.lib` 更高版本扩展映射、跨谱大库局面搜索、VCT 引擎和图片识谱；大型棋谱现已使用 Worker + IndexedDB 紧凑索引，并在启动时只载入摘要、打开时按需恢复节点。

## 明确边界

- 禁手提示是移动端快速辅助，并非赛事裁判级完整判定器。
- `.lib` 已可导入主线、变化与常见注释；超出当前版本边界的扩展字段会给出提示并保守跳过。
- 当前保存发生在设备本地；在云同步上线前，可使用设置里的"一键备份"整包导出，或定期导出 `.renju` 备份重要研究。
