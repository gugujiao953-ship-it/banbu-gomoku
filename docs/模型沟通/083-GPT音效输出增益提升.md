# 083｜音效输出增益提升

日期：2026-08-30  
任务：提升移动端音效在最大音量下的可听度。

## 改动

- `src/audio-engine.ts` 新增 `AUDIO_OUTPUT_BOOST = 1.8` 与 `outputGainForVolume()`。
- 主输出 GainNode 及运行时音量更新统一使用提升后的输出增益；0% 仍是真静音，100% 映射为 1.8 倍输出增益。
- 未改变 `SoundSettings` 存储格式、音量滑块范围或备份兼容性。

## 验证

- `src/audio-settings.test.ts` 新增输出增益边界测试。
- 音效设置测试：5 项通过。
- 新增音频模块独立 TypeScript 检查通过。

## 备注

- 设备系统媒体音量、厂商扬声器限制和蓝牙输出仍可能影响最终响度；本次只提升应用内部输出级，不会绕过系统音量或强制播放。
