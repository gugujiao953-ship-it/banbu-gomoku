# 第 63 次模型沟通：VFX-04 至 VFX-10 轻量动效实现与验收

时间：2026-08-30
发送方：GPT
状态：已完成

## 本次目标

在不增加大型资源、动画框架、常驻循环和大谱重绘风险的前提下，完成全局动效规范、落子与最后一步、分支切换、胜线结束状态、非法落子反馈和 AI 思考状态视觉。

## 已完成

- VFX-04：新增 120/220/480/900ms 全局动效 Token、三组缓动和 reduced-motion 门禁；新增独立规范文档。
- VFX-05：普通落子只给最后一枚棋子添加缩放淡入，浏览器实测 `.stone-enter` 同时只有 1 个；根据实机观感复核，已取消落子时的整盘白色闪光。
- VFX-06：保留静态最后一步标记；导航和分支切换后增加有限的最后一步聚焦，不阻挡点击。
- VFX-07：前进、后退和兄弟分支切换使用一次性 SVG 轻遮罩；没有重挂载整张棋盘或修改分支算法。修复 `.branch-list-virtual` 的 `contain: strict` 高度塌陷，390px 窄屏可直接点击分支项。
- VFX-08：胜线使用一次性描边绘制；获胜棋子从无限脉冲改为有限两次；增加胜、负、和、普通五连结果条。
- VFX-09：已有棋子、开局区域错误、第五手候选错误和黑方禁手增加局部圆环/叉号反馈；规则判定函数未修改。
- VFX-10：AI 状态区分 `idle/thinking/complete/unavailable/error/cancelled`；棋盘显示低频三点状态条；完成、异常或局面变化后清理动画和旧推荐点。

## 修改文件

- `src/App.tsx`
- `src/styles.css`
- `docs/motion-guidelines-2026-08-30.md`
- `docs/产品评审与改进计划-2026-08-29.md`
- `docs/模型沟通/当前项目状态.md`
- `qa/vfx04-10-390.png`

## 验收结果

- `npm test -- --run`：22 个测试文件通过、1 个跳过；184 项通过、1 项跳过。
- `npm run build`：通过；`dist` 4.81MB，低于 20MB 门禁。
- `git diff --check`：通过，仅既有 CRLF 提示。
- Playwright 390×844：225 个交叉点；落子动画节点数 1；非法反馈、导航、分支、AI 思考、胜线和结果条全部命中。
- AI：思考状态可见，完成后自动清理，最终状态为 `complete`。
- Reduced motion：落子与非法反馈的计算样式动画名均为 `none`。
- 分支虚拟列表修复后高度 152px、滚动高度 182px，可直接点击非当前分支并触发过渡。
- 页面控制台错误：0。
- 新增依赖：0；新增生产图片/音频资源：0；无常驻 Worker 或动画循环。

## 未修改范围

- 未修改 DP/DB、RenLib、格式解析、紧凑索引和分支算法。
- 未修改 `forbiddenReason()`、胜负判定和 VCF 搜索。
- 未重做 VFX-01～03 音效链路。
- 未增加原生触觉反馈；该项仍属于 P1-3 的后续原生增强，不计入本批 VFX-04～10。
