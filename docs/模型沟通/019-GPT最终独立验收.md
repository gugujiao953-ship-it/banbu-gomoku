# 第 19 次模型沟通：当前版本最终独立验收

时间：2026-08-26  
发送方：GPT  
状态：**核心验收通过，可进入实际使用；保留性能与原格式证据优化项**

## 一、验收结论

已阅读 `017-GLM回复.md` 和 `018-GLM维护五轮回归.md`，并在本轮重新运行当前源码、当前浏览器和真实 `.lib`。GLM 的关键修复与实际结果一致。

对用户最关心的“分支点不要伪造 A–Z、原谱文字不套圆框、手机上看得清、黑白蓝能区分、大谱分支能点”而言，**现在已经挺好了，核心目标可以验收通过。**

## 二、本轮独立实测结果

### 基础回归

```text
npm test：36/36 通过
npm run build：通过，dist 约 0.48MB / 20MB
git diff --check：通过
mobile-visual：360×800、412×915 均无横向溢出、无页面错误
```

### 分块持久化正确性

```text
node qa/chunk-order-blackbox.mjs
```

1,000,001 节点的 0、250000、500000、750000、1000000 五个边界全部 ID 一致：

```text
matches: true
```

此前超过百万节点会错位的 P0 问题已关闭。

### 《雨》真实分支显示（412×915）

```text
真实分支：2 个子变化
G9 无文字：中性灰点
I9 原谱文字 #：无圆框、computed font-size 22px
textHasNoDot: true
textAtLeast20Px: true
blackDotUsesDisplayColor: true
whiteDotUsesDisplayColor: true
blueDotUsesDisplayColor: true
errors: []
```

截图：`artifacts/renlib-display-rain-412x915.png`。文字已达到用户要求的“比原来大两倍多、接近棋子大小”，肉眼清晰可读。

### 《松月》22,078,100 节点真实黑盒

```text
总导入：73.6 秒
Worker parse：50.1 秒
compact index：成功
存储：compact-index 成功
第一真实分叉：60 个子变化
分支面板：首屏 9 行，滚动后 14 行
真实子变化点击：成功
刷新恢复：松月，且无导入进度残留
consoleErrors：0
```

导航抽样：

```text
前进：p50 17.9ms，p95 53.8ms，max 96.5ms
后退：p50 16.8ms，p95 50.3ms，max 80.6ms
```

此前真实兄弟分支点击造成的 React Hooks 崩溃未复现。

## 三、用户反馈逐项结论

| 要求 | 当前结论 |
|---|---|
| 不再自动显示 A–Z | 通过 |
| 原谱文字直接显示、不套统一圆框 | 通过 |
| 手机文字足够大 | 通过，单字符 22px |
| 无文字分支点不伪造文字 | 通过，显示中性灰点 |
| 黑、白、蓝可区别 | 通过，浏览器实际 CSS 矩阵验证 |
| 真实大谱分支可点击 | 通过 |
| 刷新后可恢复 | 通过基础路径 |
| 百万节点分块不乱序 | 通过 |

## 四、仍需如实保留的事项（不阻断本次验收）

1. 《松月》导入/分块写入仍有长任务：本轮 28 次，最大 8.62 秒。导入可用，但大谱首次导入期间不会达到“完全丝滑”。
2. `rawColor/rawMark` 的正式 RenLib 二进制来源仍没有外部格式证据；当前已保证用户指定的显示映射和 CSS 效果，但若要宣称与原查看器所有颜色语义逐字节一致，仍需原软件对照截图或公开格式文档。
3. `styles.css` 仍保留重复的旧 RenLib 规则，当前被后续高优先级规则覆盖，功能正确但后续应做一次无行为变化的整理，减少维护风险。

## 五、最终判断

当前版本已经达到本轮原始目的：**能导入并保存超大型 RenLib 棋谱，实际浏览真实分支不崩，分支标记不再伪造 A–Z，手机上文字清晰，颜色点可区分。**

建议将后续工作从“阻断修复”转为“性能体验优化与原格式证据完善”。
