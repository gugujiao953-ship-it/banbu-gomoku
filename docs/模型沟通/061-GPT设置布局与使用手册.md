# 061｜设置布局与使用手册

日期：2026-08-29

## 本轮完成

- 设置页整理为“使用手册与反馈”优先、核心偏好、可选增强、设备布局的阅读顺序。
- “使用手册与反馈”默认展开，用户进入设置后即可看到使用手册入口，不需要先展开隐藏分组。
- 新增“使用手册”底部弹窗，共 21 个独立折叠条目。
- 每个条目使用实际界面中的 Lucide 图标作为图标示例，并包含功能目的、操作步骤和注意事项。
- 手册覆盖打谱、棋盘、创建、保存、导入、导出、棋谱库、文件夹、注释、标注、导航、分支、查找、AI、局面思考、备份、主题音效、无障碍、图片识谱、格式边界、回收站和反馈更新。

## 修改文件

- `src/App.tsx`
- `src/styles.css`

## 验收

- `npm run build` 通过。
- dist 体积约 4.91MB，低于 20MB 门禁。
- 390×844 浏览器验证通过：使用手册入口可见、设置帮助分组默认展开、21 个手册条目存在、条目可展开、弹窗整体可滚动、关闭后弹窗移除、页面无运行时错误。
- `git diff --check` 无空白错误。

## 设计边界

- 手册不另建图片图标资源，直接复用生产界面真实图标，避免图标与实际操作不一致，也不增加资源包体。
- 手册使用原生 `details/summary` 折叠结构，支持键盘和浏览器默认语义。
- 桌面宽屏下手册条目使用双列布局；移动端保持单列，方便拇指操作和连续阅读。
