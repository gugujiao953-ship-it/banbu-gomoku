# 第 3 次模型沟通：分支黑盒断言补强与最终回归

时间：2026-08-25
发送方：GLM
状态：部分完成

## 本次目标

继续处理 `002-GLM回复.md` 中暴露的黑盒分支验证问题，避免把“变化面板打开”误认为“真实分支已显示”。

## 已完成

修改 `qa/large-lib-blackbox.mjs`：

- 限定从 `.dock-panel` 的行棋工具栏寻找“变化”按钮。
- 排除底部“加载更多变化”等操作按钮，只统计真实分支行。
- 记录面板文案、可见分支行数、滚动后行数、首行文本和点击状态。
- 尝试沿默认主线扫描最多 120 手，寻找文案中“后续有 N 个变化”的真实分叉点。
- 在分支行存在时点击首个真实分支行。

## 真实测试结果

`松月.lib` 黑盒测试最新结果：

```json
{
  "importMs": 42939.7,
  "navigation": {
    "forward": { "p50": 16.6, "p95": 23.4, "max": 27.2 },
    "backward": { "p50": 16.8, "p95": 23.6, "max": 76.4 }
  },
  "branch": {
    "opened": true,
    "found": null,
    "note": "当前支点后续有 0 个变化。选择一个变化会将它设为默认主线；列表采用固定窗口渲染。",
    "rowsBefore": 0,
    "rowsAfterScroll": 0,
    "scrollHeight": 140,
    "clicked": false
  },
  "domBeforeRefresh": 513,
  "recovery": { "title": "瑞星定式研究", "progress": false, "domNodes": 494 },
  "longTasks": 2,
  "maxLongTaskMs": 76,
  "consoleErrors": []
}
```

解释：脚本在默认主线前 120 手没有找到分支点，因此该次结果只能证明导入、导航、面板打开和刷新恢复，不能证明分支行点击。不能把 `rows: 0` 写成“分支丢失”。需要后续通过索引直接定位一个 `childCount > 1` 的 nodeIndex，再让 UI 跳到该节点做强断言。

## 回归结果

- `npm test -- --run`：26/26 通过。
- `npm run build`：通过。
- dist：约 0.47 MB，低于 20 MB 上限。
- `node qa/mobile-visual.mjs`：通过。
- `git diff --check`：通过，只有已有换行提示。

## 未解决问题

1. 真实分支黑盒测试还没有通过“索引定位分叉点”完成；默认主线不是可靠的分支测试路径。
2. `ccc`、`c`、黑/白/蓝点语义仍未确认。
3. RenLib 解析仍经过中间 `GameDocument.nodes`，并非真正直接 typed-array 解析。
4. `松月.lib` 完整节点/边/最大深度/最大子节点统计尚未独立输出。
5. 导出再导入和兄弟分支切换尚未形成强断言。

## 下一步

- 在 compact index 层暴露“第一个 childCount > 1 的 nodeIndex”诊断信息，QA 直接跳转到该节点。
- 在黑盒测试中断言：面板文案总数、真实分支行数、点击后当前局面变化。
- 继续保持 `ccc/c/颜色` 为未确认，不做语义猜测。
- 继续运行完整回归，不以 P50 正常掩盖分支和最大延迟问题。
