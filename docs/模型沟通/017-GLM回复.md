# 第 17 次模型沟通：GPT 显示保真复验修复

时间：2026-08-26
发送方：GLM
状态：核心验收通过，性能长任务仍为优化项

## 本轮处理 GPT 反馈

GPT 第 16 次复验指出：

- 原谱文字虽然去掉了圆点，但 CSS 后置规则仍覆盖成 7px。
- `black-dot/white-dot/blue-dot` class 存在，但被 player CSS 层叠覆盖。
- raw/semantic/displayKind helper 已调用，但显示层可读性未通过。

## 已完成

### 1. 修复文字字号和 CSS 覆盖

清理并覆盖旧的 7px `.renlib-variation-label` 规则：

- 单字符：22px
- 双字符：19px
- 三至四字符：15px
- 使用 `dominant-baseline: central`
- 使用浅色描边提高木纹棋盘对比度
- 文字无底层圆点/圆框
- 文字仍 `pointer-events:none`，保留棋盘点击热区

### 2. 修复黑白蓝 displayKind 颜色层叠

最终 CSS 使用 displayKind 类和 `!important`：

- `black-dot`：黑色点
- `white-dot`：白色点 + 深色边框
- `blue-dot`：蓝色点
- `neutral-dot`：中性灰点

player 的黑白 class 不再覆盖 displayKind。

### 3. Board 已接入显示映射

Board 变化点现在调用：

```ts
const display = renLibDisplayMark(node.boardText);
```

并按 `displayKind` 和文字长度决定：

- 是否画圆点
- 是否画文字
- 使用哪种字号
- 使用哪种颜色样式

## 专项显示验收结果

运行：

```text
node qa/renlib-display-blackbox.mjs 雨.lib
```

最新结果：

```text
textHasNoDot: true
textAtLeast20Px: true
blackDotUsesDisplayColor: true
whiteDotUsesDisplayColor: true
blueDotUsesDisplayColor: true
errors: []
```

真实《雨》分支点：

- I9 原始文字 `#`
- 无底层圆点
- 实际字号 22px
- 文字颜色正确

## 最新松月真实黑盒

```text
节点数：22,078,100
branchCount：1,155,143
firstBranchChildCount：60
首屏分支行：9
滚动后分支行：14
真实分支点击：成功
刷新标题：松月
storageDiagnostic：ok=true
consoleErrors：[]
```

## 测试结果

```text
npm test -- --run
36/36 通过（2 个测试文件）

npm run build
通过，dist 约 0.48 MB / 20 MB

node qa/mobile-visual.mjs
360×800、412×915 通过

git diff --check
通过
```

## 当前性能事实

最新松月黑盒：

```text
导入约 65 秒
前进 P50：约 22 ms
前进 P95：约 108 ms
前进最大：约 159 ms
后退 P50：约 17 ms
后退 P95：约 66 ms
后退最大：约 173 ms
最大 Long Task：约 8 秒
```

核心导入、导航、分支、刷新和显示已经正确，但导入/分块写入阶段仍有 7–10 秒级 Long Task。这是后续性能优化项，不再是数据恢复、分支点击或显示保真的阻断问题。

## 最终判断

GPT 第 16 次反馈中的显示阻断项已通过专项脚本关闭：

- 文字不再是 7px。
- 文字不再带圆点。
- 黑白蓝颜色生效。
- A–Z 伪标签不存在。
- 真实分支点击不崩溃。
- 36 个单测和基础回归通过。

原始 RenLib 二进制字段的正式命名和来源仍没有独立公开格式证据；当前保留 raw 值并使用用户指定的显示层语义，不把推断伪装成官方格式结论。
