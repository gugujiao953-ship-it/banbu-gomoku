# 099 - 手册同步 v1.1.6+ 实装能力与测试脚本维护验收（2026-09-04）

## 背景
用户指令：停止继续做评审报告中的后续项目，转为「验收已做内容 → 更新设置内使用手册 → 测试 bug → 更新图谱 → 更新 GitHub」。

## 验收基线与结果
- 仓库：`D:\Projects\五子棋2`，HEAD 从 616e2ab（v1.1.6 基线快照，含并行会话未提交的返回键/覆盖层文件收编）推进到 b638fc1 并已推送 origin/main（私有远端 banbu-gomoku-src，代理 7897）。
- 单测：`vitest run src` = 66 文件 / 330 用例全绿（与 run-qa smoke 口径一致）。
- 类型：`tsc -b` 通过。
- 浏览器正式回归：20/20 全绿（首轮 5 失败 → 全部定位为「测试脚本滞后」而非产品 bug，见下）。
- 视觉：无产品 bug 发现；一次 visual-ui mobile-390 的 255ms 长任务经单跑复测（6/6 通过、最大 170ms）确认为并发负载偶发，非回归。

## 测试脚本维护（本次改动）
1. `scripts/verify-ai-rules.mjs`、`qa/ai-opacity-rules-cancel.mjs`（两处 context）、`qa/export-content-verify.mjs`：补 `banbu-first-run-welcome-v1` 跳过键。根因：run-qa.mjs 走 `?qa=1` URL 跳过首运行引导，但直接单跑脚本时无该参数，FirstRunWelcome 遮罩挡死所有点击（首轮 run-qa 内通过、单跑失败即是此因）。
2. `qa/mobile-visual.mjs`：导航行（起点/上一手/…）已移出 `.dock-panel`（App.tsx:4155 命令栏容器），选择器去掉 `.dock-panel` 前缀并用 `exact:true`；这是 092 读谱紧凑布局重构后的真实漂移。
3. `src/features/manual/UserManual.test.tsx`：断言 19 节并新增 VCF 出题/索索夫-8/十项文案检查。

## 使用手册更新（UserManual.tsx，18→19 节）
- 新增「VCF 出题训练」节（做题 dock VCF 面板：变形/原创、短/中/深、1/5/10、逐题翻页、解答、导出题集 JSON——评审曾误列为缺口，实际已实装）。
- AI 规则目录更正为十项（补山口、索索夫-8；前四无禁、后六有禁）——评审初稿「Soosyrv-8/Yamaguchi 未实装」结论有误，已按 opening-rules.ts:72-77,102-105 与测试更正。
- 思考补胜率/Top-3/主变化；新增棋谱内查找、跨谱查找条目。
- 做题补摆题/应战、成对悔棋与重启、翻题与进度；棋谱库补手动排序；导出补 POS；备份补 ZIP。
- 主题/棋盘/棋子数量口径对齐 22/14/16；标注类型与高亮写全（结论记号、七形状、十二色、四档高亮）。
- 图片识谱表述收口：「以十五路为主，也尝试十三/十九/十七/九路」——不再承诺任意 5–21 路（App.tsx:3574 实际只试 [15,13,19,9,17]）。
- 补快捷中心（QuickDrawer）、外接键盘左右方向键。

## README
- 开局规则清单补全十项；「禁手裁判」修正为「禁手辅助提示」（与 PRODUCT_NOTES 实现边界一致）。

## 图谱
- `graphify-out/` 全量重建（范围 src，198 文件，197 code + 1 doc）：2024 节点 / 4942 边 / 98 社区，graph.html 与 GRAPH_REPORT.md 已刷新；manifest 198 条。
- 健康检查如实记录：151 条悬空边（语义→AST 跨源 id 差）、57 条折叠边（同对多行导入去重），post-build 图可用。
- God Nodes 量化确认核心问题：`App()` 222 边、横跨约 43 社区（评审中「App.tsx 巨石化」有了直接图谱证据）。
- 敏感跳过：`src/design-tokens.css`（密钥正则误判，CSS 本走 AST 不受影响）。

## 未做/边界
- 未动任何产品 src 行为（仅手册文案与测试脚本）；`design-lab/`、看门狗脚本删除属并行会话，未纳入本次提交。
- 评审报告中剩余的 P1/P2 研究纵深项（开局库、VCT、胜率曲线、学习闭环调度等）按用户指示暂缓，不在本次范围。
- 本目录外文件（15_半步五子棋 评审 md、截图）不属于 git 仓库。
