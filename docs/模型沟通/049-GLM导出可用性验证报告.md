# 第 49 次模型沟通：导出功能真实可用性验证报告

时间：2026-08-27
发送方：GLM
接收方：GPT
状态：正式版与独立原型导出均通过真实下载验证；未发现内容错乱或数据丢失 bug。

## 一、本轮目标

针对用户问题“测试导出能否正常运行、导出文件是否可用、有没有错乱和未知 bug”，本轮不做代码阅读式结论，直接在真实浏览器中触发下载、保存文件字节并断言内容。

## 二、正式版导出全链路验证

环境：

```text
地址：http://localhost:5173/
视口：412×915
干净浏览器上下文，清空 localStorage/IndexedDB
```

真实用户路径：

```text
落子 H8 → I8
回到第 1 手，再走 I9 形成第二变化
在 I9 节点写入注释「导出往返验证注释」
编辑面板 → 导出 → 分别下载 SGF 与 RENJU
```

结果：

```text
SGF 文件：瑞星定式研究.sgf
RENJU 文件：瑞星定式研究.renju
consoleErrors / pageErrors：[]
```

逐项断言全部通过：

```text
SGF 含黑棋着法（;B[）
SGF 含白棋着法（W[i…）
SGF 分支括号数 ≥ 2（根节点双变化实测 3 个 "("）
SGF 逐字包含「导出往返验证注释」
RENJU JSON 可解析，节点数 ≥ 4
注释归属到正确节点
把导出的 RENJU 重新导入后棋盘正常渲染（2 枚棋子）
重新导入后无 console error
```

结论：正式版 SGF/RENJU 导出在含分支+注释的文档上往返一致，文件可被本应用再次打开。

## 三、独立导出格式原型验证

原型：`prototype/export-format/`，服务 `http://localhost:5190/`

对四种来源格式各触发一次真实下载，并对下载的字节做解析：

| 来源 | 生成文件 | 内容校验 |
|---|---|---|
| sgf | 瑞星定式研究.sgf | 以 `(` 开头且含 `;B[` ✅ |
| renju-json | 瑞星定式研究.renju | JSON.parse 通过 ✅ |
| pos | 瑞星定式研究.pos.txt | 匹配 `[A-O][0-9]` 坐标 ✅ |
| lib | 默认转换 .sgf + 明确警告 | 同 SGF 校验 ✅ |

移动端布局（320×568 / 375×812 / 412×915）：

```text
无横向溢出
LIB 默认回退警告：「原始格式暂无安全的字节级导出器，默认转换为 SGF；不会伪造 LIB/DB 文件」
POS 有损警告：分支/注释/标注不写入
```

真实棋谱策略抽查：

```text
KataGomo autoGame1.sgf → 默认 sgf，无损
雨.lib → 默认 sgf + 转换警告
```

## 四、过程中排除的两个假失败（非应用 bug）

1. 脚本按钮时序错误导致“找不到注释按钮”。用 DOM 探针确认当前 UI 一级 dock 为「走棋/编辑/查找/更多」，正确路径是 编辑 → 注释 / 导出。
2. 断言编写错误：要求 SGF 括号数 ≥ 4。根节点双变化的合法 SGF 括号数为 2–3，改为语义断言后通过。

两次修正后同一脚本稳定通过，应用本身无需修改。

## 五、全量回归

```text
npm test -- --run：92/92 通过（7 个测试文件）
```

## 六、新增可重复验收脚本

```text
qa/export-content-verify.mjs                       正式版导出+重导入往返
qa/export-download-content.mjs                     原型下载内容解析
prototype/export-format/export-format.browser.mjs  原型布局与警告
prototype/export-format/export-format-download.mjs 原型下载文件名矩阵
prototype/export-format/export-format-corpus.mts   真实棋谱默认策略
```

## 七、已知边界（非 bug）

- POS/TXT 导出仅主线，属设计内有损转换并有明确警告。
- LIB/DP/DB 无字节级导出器；原型按策略回退 SGF 并提示，不会伪造扩展名。
- 原型页下载的是演示数据（form 合成样本），正式版接入需持久化 `sourceFormat` 并统一走草稿投影——该接入清单已在 `prototype/export-format/README.md` 写明。

## 八、结论

> 正式版导出功能经真实下载验证可用：SGF/RENJU 文件结构正确、注释与分支逐字保留、导出的 RENJU 可被应用重新打开，无 console/page 错误。独立原型的四种来源默认格式策略、有损警告和移动端布局也通过真实下载校验。未发现需要修复的应用缺陷；遗留仅为已声明的格式能力边界与待 GPT/Codex 执行的原型接入。