# GLM｜并发会话 P0 执行记录与对账

日期：2026-08-29
发送方：GLM（ZCode 会话 sess_758e3343，继承 sess_8f38fb04）
性质：与 GPT 会话并行执行《产品评审与改进计划-2026-08-29》，本文对账两条并发线的产出，避免重复实现互相覆盖。

## 1. 并发对账结论（先读这里）

| 计划项 | GLM 本线 | GPT 并发线（docs 054–057） | 合并状态 |
|---|---|---|---|
| P0-1 AI 强度基准 | VCF 专项 0/30（高级题，maxSteps=15），大预算诊断佐证 | 056：确立 Rapfi 档位口径，高级题 19/30（63.3%）；重写 benchmark 工具 | **以 056 Rapfi 口径为准**；GLM 的 VCF 工具已移入 `qa/archive/`（vcf-strength-*），VCF 0/30 仅作 VCF 专项结论保留 |
| P0-2 禁手黄金用例 | 在既有用例上追加 9 个黄金局面（边界长连×3、边界恰五、独立跳三、假活三×2、边界连三、四三合法）+ 1 个禁手点分类 API 测试 | 055：用例扩到 25 个，并修复 `game.ts` 延伸点三三漏判 | 两者叠加：`src/renju-rules.test.ts` 35/35 通过（22:26 与 22:57 两次验证） |
| P0-3 文档口径 | README（5–25 路、功能表 8 项刷新、VCF 表述）、帮助页"十五路边界"改"棋盘路数边界"、PRODUCT_NOTES 备份表述 | 未涉及 | GLM 完成 |
| P0-4 dev renlib 资产 | 重构 `vite.config.ts`：清单共用 + `configureServer` 中间件（curl 验证 200/MIME 正确） | 057：在 GLM 实现基础上补未知资源 404（修掉 SPA fallback 200），并完成浏览器级验收 | 两者叠加完成，GPT 线已验收 |
| P0-5 ErrorBoundary | 全新实现：`src/ErrorBoundary.tsx` + `src/diagnostics.ts`（操作环形日志 60 条 + 诊断导出）+ main.tsx 挂载 + 注入开关 `__banbuForceRenderError` + 5 个单测 | 未涉及 | GLM 完成 |
| P0-6 qa 治理 | 未做（发现并发线在做，主动避让） | qa/README、qa:smoke/browser/full、manual/archive 分层 | GPT 线完成 |
| P1-1 分享图片 | 未做 | 054 + @capacitor/share | GPT 线完成 |
| P1-12 依赖固定版本 | 未做 | 未做（package.json 仍是 `latest`×7） | **待办** |

## 2. GLM 线变更清单

- `scripts/benchmark-ai.mjs`：maxSteps 10→15（后被 056 的重写取代）。
- `src/renju-rules.test.ts`：+9 黄金局面、+1 禁手点分类 API 测试（叠放在 055 之上）。
- `README.md`：5–25 路口径、已实现清单补 8 项（VCF 搜索、AI 思考、人机、开局规则、暗色主题、备份、分享图、图片识谱）、SGF 拒绝边界改写。
- `src/App.tsx`：帮助页"棋盘路数边界"文案；`recordAction` 埋点 6 处（导入成败、保存、备份、恢复、图片识谱）；`__banbuForceRenderError` 注入开关。
- `PRODUCT_NOTES.md`：一键备份表述对齐。
- `vite.config.ts`：RenLib 资源清单抽出 + dev 中间件（057 在此之上增强）。
- 新增：`src/ErrorBoundary.tsx`、`src/diagnostics.ts`、`src/error-boundary.test.tsx`、`src/main.tsx` 挂载、styles.css 错误卡片样式。
- 归档：`qa/archive/vcf-strength-benchmark.test.ts`、`qa/archive/vcf-strength-report-2026-08-29.json`（附废弃说明）。

## 3. 验证状态（GLM 线，合并工作区）

- 类型检查：`npx tsc --noEmit` 通过（23:00，合并工作区）。
- 定向回归：`renju-rules.test.ts` + `error-boundary.test.tsx` 40/40 通过。
- P0-1 专项：VCF 0/30（700ms/70000 节点）与 0/30（10s/200 万节点，19 题 budget + 11 题 not-found）两档均留档。
- 全量 `npm test` / `npm run build`：**未在合并工作区复跑**（GPT 线仍在活跃写入，结果不归因）；建议双方停止并发编辑后跑一次完整回归。

## 4. 交接提醒

1. 两条并发线在同一工作区活动约 1 小时，靠"发现已有实现则叠加、不覆盖"的约定避免了对冲；后续会话请延续这一约定。
2. ErrorBoundary 的浏览器级验收方式：Playwright `addInitScript` 设置 `globalThis.__banbuForceRenderError = true` 后打开页面，应显示崩溃卡片而非白屏；`导出诊断信息` 下载 JSON。
3. P1-12（依赖固定）是当前唯一无人认领的 P0/P1 快赢项；package.json 正被 GPT 线编辑，建议等其告一段落再做。
4. `docs/模型沟通/当前项目状态.md` 本轮未由 GLM 更新，避免与 GPT 线冲突；建议以本文 + 054–057 为准先行对照。
