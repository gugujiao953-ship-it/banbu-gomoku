# 090｜全量测试与融合验收

日期：2026-08-31

## 验收结论

当前工作区已完成并行改动融合复核。未执行 `git reset`、`git checkout` 或清理未跟踪文件；现有功能代码、测试、文档与 QA 证据均保留。

## 实际命令与结果

- `npx tsc --noEmit`：通过。
- `npm test -- --run`：56 个测试文件、284 项通过、1 项跳过。一次并行负载下 VCF 测试瞬时返回 `budget`，独立重跑和串行 `qa:full` 均通过，未复现。
- `npm run qa:full`（`QA_BASE_URL=http://127.0.0.1:5173/`）：通过。确定性门禁 52 个文件/259 项通过；浏览器回归覆盖导入、导出、最近导入、草稿、AI 规则/无限思考取消、棋谱树/分支、数据安全、设置、外观与音效、棋盘/棋子透明度、360/390/412px、横屏、平板、深色、减少动效、ErrorBoundary、RenLib 资源及构建体积门禁。
- `npm run build`：通过，现代、legacy、PWA 产物均生成，`dist` 约 6.31MB（上限 20MB）。
- `git diff --check`：通过，仅有共享工作区既有 LF/CRLF 提示。
- 生产专项：`QA_BASE_URL=http://127.0.0.1:5181/ node qa/settings-page-regression.mjs` 与带 `?qa=1` 的 `qa/ai-opacity-rules-cancel.mjs` 均通过；`http://127.0.0.1:5181/` 返回 200，现代模块、legacy nomodule 和 manifest 均可见。

## Android 与真机边界

当前环境无 `adb`/`hdc`，Java 8 的 `JAVA_HOME` 也无效，因此本轮未重新打包或安装 APK。现有 v1.1.6 APK、Android legacy 构建配置与既有 Redmi K20 Chromium 83 证据仍有效，但不把它们表述为本轮新安装验证。

> 后续更新：第 91 轮已定位到项目内 `artifacts/tools/jdk21`、`artifacts/tools/android-sdk` 和 `platform-tools/adb.exe`，并完成正式 APK 构建及 Redmi K20 Pro 覆盖安装、冷启动与持久化验收。当前结论以 [091-GPT-v1.1.6-APK打包与真机验收.md](./091-GPT-v1.1.6-APK打包与真机验收.md) 为准。

## 后续建议

1. 在具备 JDK 21、Android SDK 与真机工具的环境重新执行 `npm run android:release`，完成 v1.1.6 安装/升级/冷启动验证。
2. 继续关注 VCF 在高并行 CPU 负载下的时间预算抖动；生产门禁已使用单 worker 串行确定性配置。
3. 后续继续沿用 2026-08-31 的交接日期口径，避免状态文档与单轮交接时间不一致。
