# 091｜v1.1.6 APK 打包与真机验收

日期：2026-08-31

## 打包结果

- 命令：`npm run android:release`
- JDK：项目内 Temurin 21
- Android SDK：项目内 SDK，build-tools 36.0.0
- Gradle：`assembleRelease` 成功，205 个任务中 40 个执行、165 个复用缓存
- APK：`artifacts/releases/1.1.6/banbu-gomoku-1.1.6-android-release.apk`
- 大小：5,442,296 bytes
- 包名：`cn.renjunote.mobile`
- 版本：`versionName=1.1.6`、`versionCode=8`
- SHA-256：`4BC0FD7ADF698FBE702BBB110A5499717CA3A0C059533F50C817919E78057370`
- 签名校验、包名与版本元数据校验：通过；复用既有 release 证书，没有创建新签名。

## 真机环境

- 设备：Redmi K20 Pro
- Android：11 / API 30
- System WebView：83.0.4103.106
- 安装方式：项目内 `platform-tools/adb.exe install -r`
- 安装结果：`Success`；保留既有应用数据，设备记录的首次安装时间不变，更新时间更新为本轮安装时间。

## 真机验证结果

- 覆盖安装后启动成功，`MainActivity` 为 resumed activity，无白屏。
- legacy WebView 83 正常渲染当前 UI、雨幕动态主题、棋盘、棋子、设置中心及外观与音效子目录。
- 棋盘透明度从 100% 调整至 55%，强制停止并重新启动后仍为 55%，预览层正确透出雨幕主题，网格和棋子保持清晰。
- 真机完成两手落子；强制停止后冷启动，棋谱仍显示第 2 手/2，棋子位置保持。
- 点击保存后状态变为“已保存”；再次冷启动后仍为“已保存”。
- 两次 `am start -W` 冷启动均成功，`TotalTime` 分别为 523ms、543ms。
- logcat 扫描未发现 `FATAL EXCEPTION`、本应用 ANR 或未捕获脚本异常。
- 真机证据主要保存在 `qa/device-v116-post-install.png`、`qa/device-v116-opacity-55-persisted.png`、`qa/device-v116-two-stones-persisted.png`、`qa/device-v116-saved-after-cold-start.png`。

## 边界与后续

- 本轮验证覆盖安装、旧 WebView 启动、设置/主题/透明度、落子、保存与冷启动持久化；未在真机重新执行大文件导入、系统文件选择器和系统分享目标的完整人工验收，这些路径已有浏览器与单元回归覆盖。
- Gradle 输出包含 flatDir、SDK XML 版本和 Gradle 9 兼容性警告，但不阻断当前 Gradle 8.14.3 构建。
- 本轮未提交、未推送 GitHub、未创建 Release，也未清理共享工作区的既有文件。
