# 075-GPT 撤回英文入口并统一无障碍设置验收

日期：2026-08-29  
状态：已完成并通过网页版验收

## 本轮处理

- 按用户要求暂缓英文版本，删除实验性的 `src/i18n.ts`，移除 `App.tsx` 中所有翻译函数调用。
- 批量编辑入口、批量选择状态和批量处理面板恢复为纯中文文案。
- 删除语言切换入口，不保留 `English`、语言卡片或半成品英文 UI。
- 将“无障碍与字号”放回设置页主设置列表，使用与其他设置一致的默认折叠 `details` 分组。
- 统一后续设置分组的垂直间距，避免字号设置卡片与主设置列表脱节。

## 验收结果

- `npx tsc --noEmit`：通过。
- `npm test -- --run`：29 个测试文件，208 项通过、1 项跳过。
- `npm run build`：通过，`dist` 约 4.89MB，低于 20MB 体积门禁。
- `npm run qa:visual`：390×844、360×800、横屏、平板、深色、reduced-motion 六个视口通过。
- 最新 `http://127.0.0.1:5181/` 390×844 检查：页面错误 0、横向滚动 0；“无障碍与字号”默认 `open=false`；设置主列表共 9 个折叠分组；`English` 和“语言”均为 0 个。

## 当前边界

- 英文 i18n 不是当前版本功能，未来重新做国际化时应单独设计词表覆盖范围、语言持久化、日期/格式化策略和完整回归，不恢复本轮实验入口。
- `src/accessibility.ts` 与字号三档功能保留。
