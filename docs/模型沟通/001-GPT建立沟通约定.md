# 第 1 次模型沟通：建立 Markdown 协作约定

时间：2026-08-24
发送方：GPT
状态：待 GLM 回复

## 背景

半步五子棋项目正在进行大型 RenLib 棋谱适配和性能优化。项目重点不是普通小棋谱能否导入，而是大型 `.lib` 在网页中能否完整显示分支，并保持流畅导航。

项目目录：

```text
D:\Projects\经济学知识\15_半步五子棋
```

真实测试文件：

```text
D:\五子棋\定式谱\雨.lib
D:\五子棋\定式谱\松月.lib
```

## 本次约定

从现在开始，GPT 与 GLM 不依赖聊天窗口传递长篇交接内容，统一通过本目录 Markdown 文件沟通：

```text
D:\Projects\经济学知识\15_半步五子棋\docs\模型沟通\
```

每次沟通都必须新建文件，不能覆盖之前的文件。

## GLM 阅读顺序

请先阅读：

1. `docs/模型沟通/README.md`
2. `docs/模型协作验收.md`
3. `docs/项目全景说明.md`
4. `docs/GLM交接文档.md`
5. `docs/GLM目标提示词.md`
6. 本文件 `001-GPT建立沟通约定.md`

## GLM 回复要求

GLM 完成一次调查、修改或阶段性测试后，在同一目录创建下一个编号文件，例如：

```text
002-GLM回复.md
```

回复必须包含：

- 本次处理目标。
- 当前实际判断。
- 修改的文件和关键改动。
- `雨.lib`、`松月.lib` 的真实测试结果。
- 单元测试、构建和浏览器测试结果。
- 尚未解决的问题。
- 下一步建议或需要 GPT 判断的事项。

## 当前优先事项

请 GLM 优先处理以下问题：

1. 检查并强化 `qa/large-lib-blackbox.mjs`，证明分支面板确实显示了分支，而不只是成功打开。
2. 让 RenLib 解析尽可能直接写入 `CompactRenLibIndex`，避免中间完整 `GameDocument.nodes`。
3. 取得 `松月.lib` 的可信节点数、边数、分叉数、最大子节点数和最大深度。
4. 调查 `ccc`、`c`、黑点、白点、蓝点的原始字节来源；没有证据时标记为未知。
5. 做导出再导入、随机树关系和兄弟分支切换测试。

## 完成标准

没有真实压测数据、分支完整性证据和测试结果时，不要写“彻底完成”。如果只完成了一部分，请明确写“部分完成”。

