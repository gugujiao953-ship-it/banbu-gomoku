# 半步五子棋｜当前项目状态

> 这是新对话恢复项目时应优先读取的轻量交接文件。它只记录当前可执行状态，不要求读取历史聊天。
> 每次完成一轮功能修改、构建或发布后，更新本文件的“当前进度”和“交付物”部分。

## 当前基线

- 详细项目总览与历史问题基线：[051-GPT项目总览与后续维护基线.md](./051-GPT项目总览与后续维护基线.md)

- 项目目录：`D:\Projects\五子棋\项目资料\半步五子棋`
- Git 分支：`codex/features`
- v1.1.3 发布提交：`b340e49 feat: publish v1.1.3 local AI and editing improvements`
- 已发布版本：`v1.1.2`、`v1.1.3`、`v1.1.4`；当前动效修复版为 `v1.1.5`
- 旧版保留：`artifacts/releases/v1.1.0/banbu-gomoku-v1.1.0-android.apk`
- 当前版本 APK：`artifacts/releases/1.1.5/banbu-gomoku-1.1.5-android-release.apk`
- 上一版 APK：`artifacts/releases/v1.1.3/banbu-gomoku-v1.1.3-android-20260829.apk`
- GitHub Release：`https://github.com/gugujiao953-ship-it/banbu-gomoku/releases/tag/v1.1.4`（v1.1.5 APK 已本地生成，尚未上传新 Release）
- 网页预览：`http://127.0.0.1:5181/`

## 当前进度

2026-08-30 最新收口：设置页按“使用手册与反馈→思考→棋盘显示→棋盘与棋子→声音与反馈→外观主题→无障碍与字号→文件与存储→数据与兼容→关于→可选增强功能→设备布局”整理，版本标号固定在页面最底部。新增统一 Android Release 打包命令 `npm run android:release`，自动选择项目内 JDK 21/Android SDK、复用既有 release 签名，并校验 APK 元数据、签名和 SHA-256。详见 [Android发布流程.md](../Android发布流程.md)。

- 最新 Release APK：`artifacts/releases/1.1.5/banbu-gomoku-1.1.5-android-release.apk`
- APK SHA-256：`CBABA83430F033E39A691235720035D664D8FB3F94255291AE586976C72291DD`
- APK：包名 `cn.renjunote.mobile`，`versionCode=7`，`versionName=1.1.5`；签名校验通过，证书指纹与既有 release 基线一致。
- 本轮验证：动效设置与备份单测 6/6 通过，类型检查通过，生产构建通过；浏览器动效专项验证默认开启动效、手动关闭持久化和页面错误 0。完整测试基线仍需处理既有 VCF 搜索预算回归。

2026-08-30 动效修复（准备发布 v1.1.5）：发现 v1.1.4 的 `src/motion-settings.ts` 在没有配置时默认返回 `false`，导致 Android 新安装和升级后根节点进入 `data-motion="off"`，从而统一关闭落子、导航、胜线、人机思考、主题动态背景、导入进度和界面过渡。已将动效存储键升级为 `banbu-motion-enabled-v2`，默认值改为开启；旧 v1 键不会继续关闭升级后的动效。同步更新动效单测和浏览器专项回归，使用 `npm run android:release` 重新生成 v1.1.5 安装包。
- 新增交付脚本：`scripts/package-android-release.ps1`；未创建 Git 提交，未删除或覆盖并发任务的既有改动和 QA 证据。

2026-08-29 最新收口（075）：英文入口和实验性 i18n 已撤回，批量编辑恢复纯中文；“无障碍与字号”已并入设置页主设置列表，和其他设置一样默认折叠，移动端间距与视觉风格统一。最新网页版：`http://127.0.0.1:5181/`。详见 [075-GPT撤回英文入口并统一无障碍设置验收.md](./075-GPT撤回英文入口并统一无障碍设置验收.md)。

设置页与使用手册已完成（061）：设置入口按“使用手册与反馈→核心偏好→可选增强→设备布局”整理；新增 21 条可折叠使用说明，使用真实界面图标展示操作，覆盖打谱、棋盘、棋谱库、题库、AI、导入导出、备份、主题、无障碍和反馈等功能。已通过生产构建与 390×844 浏览器验收。详见 [061-GPT设置布局与使用手册.md](./061-GPT设置布局与使用手册.md)。

设置页新增“反馈与建议”入口（074）：支持问题/建议/其他三类反馈，用户可将预填内容发送到 GitHub New Issue、调用系统邮件，或复制反馈文字；邮件收件地址通过构建变量 `VITE_FEEDBACK_EMAIL` 配置，未配置时不虚构邮箱。反馈默认只附带版本、当前页面和浏览器信息，不上传棋谱、文件或本地数据。详见 [074-GPT设置反馈入口与邮件GitHub发送.md](./074-GPT设置反馈入口与邮件GitHub发送.md)。

本轮新增轻量 UI 与品牌资产：棋盘下方常驻工具栏加入“新建空白棋局”，复用现有草稿保护后创建空白棋谱；棋谱库与设置页面外层标题已隐藏，保留内部功能分组标题；新增 5 套轻量 SVG Logo 方案及预览页，暂不替换正式 Web/Android 图标，等待选择后再同步。随后完成第二批 VFX-12、13、16–20 的增量补强：统一 Toast、Bottom Sheet 焦点管理、棋盘信息层级、safe-area/横屏/平板适配、内联空状态插画、首次进入 Coach Mark，以及 Playwright 视觉与动效性能门禁。详见 `docs/logo-options-2026-08-29.md` 与 [065-GPT-VFX12至20第二批轻量UI与视觉门禁.md](./065-GPT-VFX12至20第二批轻量UI与视觉门禁.md)。

本轮继续推进 P2-3 的学习闭环子项：题库页在“导入 JSON 题库”左侧新增“错题本”入口，自动聚合已尝试但当前尚未攻克的题目，并可直接跳回原题继续做；这次不展开每日一题和题型专项。错题本数据复用现有题目进度，不额外新增存储。详见 `src/puzzles.ts` 与 `src/App.tsx`。

P2-2 棋谱树形图面板已完成首版：走棋工具栏新增“棋谱树”入口，使用独立 SVG 面板展示当前路径附近节点、连接线、黑白标识和当前节点高亮；支持节点点击跳转、键盘 Enter/空格跳转、缩放、重置和画布拖拽。普通棋谱仅展开当前路径及其直接变化，大型紧凑棋谱使用 `compactChildWindow` 按路径窗口读取，设置 240 个可视节点预算，不枚举完整 `document.nodes`。动态数据库祖先节点按深度回退，分支节点复用现有异步导航。构建 4.85MB 通过，全量测试 191 项通过、1 项跳过，390×844 浏览器回归页面错误 0。详见 [068-GPT-P2-2棋谱树形图面板实现与验收.md](./068-GPT-P2-2棋谱树形图面板实现与验收.md)。

P2-2 增量优化：棋谱树缩放上限从 180% 提升到 10×，增加缩放滑杆和 2×/5×/10×快捷倍率；缩放按钮采用分段步进，放大到高倍率后可配合拖拽查看密集分支。工具栏改为窄屏两行网格布局，390px 视口无横向溢出。节点手数改为构建窗口时一次计算，避免 SVG 每次重绘时重复调用路径深度计算。225 条根分支压力场景只渲染 13 个窗口节点，导入到打开树约 1.3 秒，页面错误 0。详见 [069-GPT-P2-2棋谱树10倍缩放与大谱性能优化.md](./069-GPT-P2-2棋谱树10倍缩放与大谱性能优化.md)。

P0-6 QA 目录治理已完成（059）：新增 `qa/README.md` 和 `scripts/run-qa.mjs`，建立确定性正式回归、真实文件手工回归、历史诊断归档三层；`mobile-visual.mjs` 已修复草稿保护与题库切换等陈旧时序假设，两个移动视口通过。详见 [059-GPT-QA目录治理与正式回归集.md](./059-GPT-QA目录治理与正式回归集.md)。

最新一轮已完成 P1-6“一键备份与恢复”（058）：新增版本化 `banbu-gomoku-backup` JSON 备份格式，覆盖普通棋谱库、棋谱/题库草稿、用户题库及进度、标题覆盖、文件夹、分支书签、主题/棋盘显示设置，以及大型棋谱 IndexedDB 摘要、文档、派生版本、草稿和紧凑索引分块。备份对 ArrayBuffer/typed array 做 Base64 编码，导入前校验 schema、版本和数据结构；大型数据库使用单事务替换，localStorage 写入失败时回滚两侧数据。设置页“数据与兼容”新增“一键备份”和“恢复备份”，恢复成功后自动重载页面。默认导出目录权限句柄不纳入备份，内置题库静态资源不重复备份。详见 [058-GPT一键备份与恢复实现及验收.md](./058-GPT一键备份与恢复实现及验收.md)。

最新一轮已经完成并通过验证的功能：

- 最近导入列表（067）：导入面板顶部显示最近 5 个棋谱/题库来源文件，使用独立 IndexedDB 持久化；小于等于 16MB 的文件支持跨页面一键重开，超过 16MB 只保留元数据并提示重新选择。重开复用正式导入、去重和草稿保护链路，默认从根局面打开；24 个测试文件 191 项通过、1 项跳过，生产构建 4.85MB 通过，浏览器专项验证持久化、重开和完整主线恢复；AI 规则回归同时确认草稿保护层优先级正确。详见 [067-GPT最近导入列表与一键重开验收.md](./067-GPT最近导入列表与一键重开验收.md)。
- 依赖固定版本（066）：将 `package.json` 全部直接依赖从 `latest`/范围版本收敛到当前 `package-lock.json` 已解析的精确版本；lockfile 根节点同步，`npm ci --dry-run`、生产构建和全量测试通过。未升级依赖，未来升级需单独建任务并重新回归。详见 [066-GPT依赖固定版本与安装复验.md](./066-GPT依赖固定版本与安装复验.md)。
- AI 强度修复、自由档与人机对局时长（065）：修复 Rapfi Worker 收到首个候选后约 80ms 提前结束搜索的问题，候选改为缓存、显式 `BESTMOVE` 或预算截止后 `YXSTOP` 才完成，并补齐定时器/旧请求清理；固定中文档位初级、中级、高级、大师开始体现时间、深度和节点差异。“自由”独立显示，支持 0.3–300 秒和 16–128 层自定义。AI 设置另新增不限（默认）、1 分钟、5 分钟、30 分钟四档对局时长；只统计人类操作时间，AI 思考、浏览历史局面和结束状态暂停，超时自动判负并停止 Worker，左上角显示累计/剩余时间。生产构建通过；184 项测试通过、1 项跳过；Rapfi、人机规则和计时专项浏览器回归均通过，覆盖 320px 窄屏、暂停/恢复、超时判负和退出清理。详见 [065-GPT人机对局时长与难度界面验收.md](./065-GPT人机对局时长与难度界面验收.md)。
- 动效开关与落子音色选择（064）：设置页“棋盘显示”新增持久化界面动效总开关，关闭后落子、导航、胜线、AI 状态、主题装饰和界面过渡停止，但胜线、非法反馈和结果信息仍静态可见；“声音与反馈”新增经典、木石、清响三套实时合成落子音色，旧设置自动兼容为经典。没有新增音频文件或动画依赖，一键备份与恢复已覆盖两项设置。全量测试 188 项通过、1 项跳过；生产构建通过，`dist` 4.82MB；Playwright 390×844 验证刷新持久化、静音不创建 AudioContext、关闭动效后胜线完整，页面错误 0。详见 [064-GPT动效开关与落子音色选择.md](./064-GPT动效开关与落子音色选择.md)。
- VFX-04～VFX-10 轻量动效（063）：新增统一动效 Token 与 reduced-motion 门禁；落子只动画最后一枚棋子；导航/分支使用一次性轻遮罩；最后一步短促聚焦；胜线一次性绘制、获胜棋子有限脉冲和克制结果条；非法点/禁手局部反馈；AI 区分思考、完成、不可用、异常和局面变化取消。修复分支虚拟列表 `contain: strict` 导致的窄屏高度塌陷。Playwright 390×844 验收控制台错误 0，reduced-motion 动画关闭；全量测试 184 项通过、1 项跳过；生产构建通过，`dist` 4.81MB。详见 [063-GPT-VFX04至10轻量动效实现与验收.md](./063-GPT-VFX04至10轻量动效实现与验收.md)。
- 历史着法点标注优先级修复（062）：返回上一局面后，选择文字标注并点击未来分支已经使用的坐标，会把标注写入当前局面，不再误跳回该步；标注结束后原分支导航与新建兄弟分支仍正常。专项浏览器回归覆盖 H8 历史点标注、H8 原分支重入、I8 新分支创建及标注保留，页面错误 0；全量测试 184 项通过、1 项跳过；生产构建通过，`dist` 4.81MB。详见 [062-GPT历史着法点标注优先级修复.md](./062-GPT历史着法点标注优先级修复.md)。
- 导入流程骨架屏与阶段进度（061）：普通棋谱、动态 LIB、DP/DB、题库 JSON 和大型后台保存统一显示“读取—解析—索引/保存—完成/失败”；单文件未知进度使用不确定条与骨架，批量文件数可计算时才显示真实百分比；大型 LIB 预览可用后明确提示后台继续建索引，不复制大型数据到 UI。`npm test` 181 项通过、1 项跳过；5MB SGF 浏览器 QA 无页面错误；生产构建通过，`dist` 4.80MB。详见 [061-GPT导入流程骨架屏与阶段进度.md](./061-GPT导入流程骨架屏与阶段进度.md)。
- 禁手黄金用例补齐（055）：`src/renju-rules.test.ts` 已扩展为 25 项测试，其中包含 22 个独立黄金局面，覆盖四方向恰五/长连、同向与异向四四、直三/跳三/假活三、边界和禁手延伸点。测试发现并修复了“成活四的唯一延伸点本身为三三禁手时仍被计为活三”的缺陷；规则、VCF、AI、全量测试和生产构建全部通过。当前仍保持“常见禁手辅助”口径，不宣称已达到 P2-7 裁判级禁手。详见 [055-GPT禁手黄金用例补齐与规则修复.md](./055-GPT禁手黄金用例补齐与规则修复.md)。
- 图片识谱重做（053）：自动网格定位（亮-暗-亮翻转特征 + 最小二乘等差拟合 + 木色包围盒约束）、饱和度+亮度两级棋子分类（黑白棋子中性灰、木色棋盘强彩色，解决灰白棋子与米色棋盘亮度重叠）、5×7 模板多尺度序号识别；序号恰为完整 1..N 时按序重建走子链，否则回退局面导入。批量验收（SGF 两局面 × 3 种截图 + 瑞星真实谱 3 深路径）位置/颜色/序号全部 100%，0 误报。详见 [053-GLM图片识谱重做与验收.md](./053-GLM图片识谱重做与验收.md)。
- DP/DB 导入分支树修复（052）：`dpCanonicalKey` 字符串比较改为参考站数值对比较；双库各 20000 局面 oracle 对照 0 失配。
- 双站对照（052）：renjutool（线上+本地克隆）vs 本项目分支坐标与标签 0 失配；白启疏星.lib（89MB）、斜月.lib（1.185GB，39 秒）专项全部 100%。
- 修复 DP/DB 导入分支树不完整：`src/formats.ts` 的 `dpCanonicalKey` 原先用字符串比较选 canonical 变换，与参考站（renjutool/Rapfi）的数值对比较在坐标 K–O 区域不一致，导致带 A 标注的分支和深层分支整支消失、终点无法切换。已改为参考站同款数值比较并补过手占位；两个真实库各 20000 局面与参考站 oracle 100% 一致。
- 双站对照（renjutool 线上站 + 本地源码克隆 vs 本项目）：分支坐标与标签在全部对照局面 0 失配；LIB（瑞星谱、雨.lib）端到端 DOM 分支标记与参考站核心 100% 一致。详见 [052-GLM导入分支完整性修复与双站对照.md](./052-GLM导入分支完整性修复与双站对照.md)。可复用的单会话双站回归脚本：`qa/manual/dual-site-oracle.mjs`（一次浏览器会话内完成参考站+本项目全部路径对照与截图）。
- 已知环境差异：`/renlib/*` 资产仅由 `npm run build` 产物提供，`vite dev` 下打开 .lib 会停在解析中；测试 LIB 请用构建预览或打包版本。

本轮之前已完成的功能（摘要）：

- 只有同一节点存在两个或以上后续着法时，才判定为分支。
- “变化”已改为“分支”相关交互。
- 支持回到上一个分支点。
- 支持按棋谱分别保存分支书签；书签可跳转、重命名、删除。
- 有书签的局面，在最新一步棋子的左上角显示分支书签图标。
- 开发预览增加了 Service Worker 重置入口，便于清除旧版缓存。
- 分支面板已将“回到上一个分支点”作为创建新分支入口；书签默认展开，保存和行内重命名可直接操作。
- “改着”和“评价”编辑入口已移除；导出移到右上角导入按钮旁，查找移入“更多”。
- 查找已覆盖普通棋谱与紧凑大型棋谱中的棋盘标注（文字、坐标和形状）。
- 全站导入统一使用向下箭头，导出统一使用向上箭头；标注入口移到棋盘工具栏，黑白落子与锁定控件靠右对齐。
- 底栏新增 AI 入口，可创建本地人机棋局并选择连珠/标准规则、是否启用黑方禁手，以及执黑先手或执白后手。
- 标注面板已调整回横向按钮布局：显示样式与颜色保持横向排列，数字、局面结论和字母按钮按一到两行紧凑排列；自定义文字独占最底部整行，常见手机高度无需滚动即可看到全部操作。
- 标注文字默认采用导入棋谱相同的中文字体与墨黑色；颜色扩展为 12 种，并保留圆圈、三角、叉号的独立样式选择。
- Rapfi 已固定使用随包本地 WASM / 数据，不再请求 `gomocalc.com`；棋局不会上传到第三方。
- 标注面板已增加“删除现有标注”红色按钮，放在自定义文字输入框下方；形状标记说明已移到棋盘标注总说明。
- 导出页已重做为“选择格式导出 / 直接导出”两级结构，并记录导入文件的原始格式。SGF 与 JSON 支持完整重编码；已打开的 LIB、DP、DB 支持原文件直接导出，但不伪造二进制转换文件。
- 设置页的导出入口和格式兼容说明已同步更新。
- 题库导入说明已改为真实格式描述：二维 JSON 数组，每题由“坐标,颜色编号”字符串组成；“关于”页已改为个人 Vibecoding 项目、侵权处理、后续维护和 GitHub 下载说明。
- 设置页已拆为“棋盘显示、文件与存储、数据与兼容、关于”四个默认收起的折叠模块，展开后才显示具体开关和按钮，并移除“打谱设置”标题。
- 棋谱信息新增开局规则字段：自由开局、五手两打、五手多打（3–10 打）、塔十（塔拉山口-10）和塔拉（五次交换）；格式说明同步补全三类基础规则与这些开局规则。
- 已移除“原创攻防体验”内置题集，并过滤旧版 localStorage 中的 `original-tactics`；空白默认棋谱不再使用“瑞星定式研究”标题，旧空白默认记录会迁移为“新建棋谱”。
- 图片识谱入口明确提示“目前准确率很低，识别后请务必人工校对”。
- 保存棋谱弹窗新增默认收起的“编辑棋谱信息”折叠面板，复用棋谱信息表单，可直接编辑棋手、赛事、规则、开局规则等字段。
- 棋谱库搜索框已提升为棋谱/题库共用入口：棋谱支持标题、棋手、主题，题库支持题集、来源、许可、题目标题、题面和先手搜索；无结果时会给出对应提示。

当前工作区有本轮待提交的源码修改和测试/工具文件，包含：

- Android 版本与 release 签名配置
- 本轮棋谱编辑、规则、人机、格式、存储和界面修改
- 对应测试、浏览器回归与 AI 基准工具
- 本地 Rapfi 运行资源

不要擅自执行 `git reset --hard`、`git checkout --` 或覆盖这些修改；先查看 diff，确认用户是否要继续当前功能。

## 验证状态

- 测试：`npm test`，`17` 个测试文件、`149/149` 通过（2026-08-29 第 55 轮复验）。
- 类型检查：`npm run build` 中的 `tsc -b` 通过。
- 网页构建：`npm run build`，通过，`dist` 约 `4.56MB`。
- 第 58 轮备份复验：`npm test -- --run`，18 个测试文件、157 项通过、1 项跳过；`npx tsc --noEmit` 通过；`npm run build` 通过，`dist` 约 `4.62MB`；`git diff --check` 通过（仅既有 CRLF 提示）。
- 本轮新增：DP canonical key 修复后，两个真实九天库各 20000 局面 oracle 对照 0 失配；DP 203 局面 + LIB 486 局面浏览器端到端 0 失配；与 renjutool（线上+本地克隆）双站对照 0 失配；`qa/dp-a-repro.mjs` 终点/A 分支回归通过。
- 本轮页面回归：维护副本独立端口验证设置模块默认收起、展开可操作、规则说明、图片识谱警示和棋谱信息开局规则下拉框；页面无运行时错误。`qa/mobile-visual.mjs` 已修复草稿保护、题库切换、面板复位和重复定位器等陈旧假设；360×800、412×915 两个视口通过，题集总数与分项数量正确，无横向溢出和浏览器错误。
- QA 目录已分层：确定性正式回归见 `qa/README.md` 与 `npm run qa:smoke|qa:browser|qa:full`；真实大文件与参考站回归放入 `qa/manual/`；历史截图探针放入 `qa/archive/`。仍在并发执行的 `qa/tmp-ai-benchmark-*` 保持原位，待对应任务完成后再收敛。
- 最新页面回归：验证保存页信息折叠、编辑字段、保存后棋谱搜索、题库搜索和无结果提示；页面无运行时错误。
- 浏览器回归：`node scripts/verify-export-annotation.mjs` 与 `node scripts/verify-ai-rules.mjs` 均通过。
- Android 构建：`assembleRelease` 通过，包名 `cn.renjunote.mobile`，`versionCode=6`，`versionName=1.1.4`。
- APK 签名：新建的本地 release 基线位于 `artifacts/signing/banbu-release-v1.p12`（忽略目录）；证书 SHA-256 为 `81:E0:EF:02:F7:B4:1E:47:3A:96:6D:C3:AE:AF:82:86:F8:BA:EE:74:91:91:B7:30:CF:E7:D9:05:06:97:A1:6D`。后续版本必须继续使用这把密钥。
- v1.1.3 APK SHA-256：`A77394D7E4F81AF73B6DA10378EB099A0DD718F04F4FA48F139A83CFE24EE151`。
- v1.1.3 本轮重打包 APK SHA-256：`73CACBD63735030E698E01980C2144FFB398073B4F162DFF5FA19427311F6B01`。
- v1.1.4 APK SHA-256：`65D943B4BB92CEBF36C583660BE36B1BD140F3F107924465AE680E952EF2F313`。
- 说明：旧 `v1.1.2` 的私钥不在当前机器；`v1.1.4` 使用与 `v1.1.3` 相同的 release 密钥，可覆盖安装 v1.1.3。
- 第 61 轮导入进度复验：`npm test` 21 个测试文件通过、1 个跳过，181 项通过、1 项跳过；`node qa/import-progress.mjs` 通过；`npm run build` 通过，`dist` 4.80MB；`git diff --check` 通过（仅既有 CRLF 提示）。
- 第 63 轮 VFX-04～10 复验：`npm test -- --run` 22 个测试文件通过、1 个跳过，184 项通过、1 项跳过；`npm run build` 通过，`dist` 4.81MB；`git diff --check` 通过（仅既有 CRLF 提示）；Playwright 390×844 覆盖落子、非法点、导航、分支、胜线、AI 状态和 reduced-motion，页面错误 0。
- 第 64 轮动效与音色设置复验：`npm test -- --run` 23 个测试文件通过、1 个跳过，188 项通过、1 项跳过；`npm run build` 通过，`dist` 4.82MB；`git diff --check` 通过（仅既有 CRLF 提示）；Playwright 390×844 覆盖三套音色、刷新持久化、静音 AudioContext 门禁、动效关闭和静态完整胜线，页面错误 0。
- 第 65 轮 VFX-12、13、16–20 复验：新增统一 Toast（保留 `.toast` 兼容选择器）、Bottom Sheet Esc/焦点循环/焦点恢复、棋盘视觉层级、safe-area/横屏/平板 CSS、内联 SVG 空状态、受控 Coach Mark 和视觉回归门禁；`npm run build` 通过，`dist` 4.83MB；`npm test -- --run` 188 项通过、1 项跳过；`npm run qa:visual` 6 组通过；`npm run qa:browser` 全部通过，页面错误 0、无横向溢出。
- 第 66 轮去除走棋导航闪烁：上一手、下一手、起点、终点、分支跳转不再显示棋盘闪屏或最后一步聚焦放大；导航前会清理残留落子动画，正常落子反馈仍保留。`npm run build` 通过，`dist` 4.85MB；390×844 Playwright 专项确认导航后的闪屏、聚焦和落子动画节点均为 0。
- 第 67 轮最近导入收尾复验：补齐草稿保护弹层 `z-index: 100` 的实际 class，最近导入专项与 AI 规则专项已纳入 `qa:browser`；聚合浏览器回归通过，覆盖导入、编辑、导出、移动端、横屏、平板、深色和 reduced-motion，页面错误 0。

### 第 71 轮｜注释常驻显示（2026-08-29）

- 注释显示改为独立开关：点击一次打开后，上一手/下一手和分支导航不会自动收起；再次点击同一按钮才关闭。
- 导航到没有注释的节点时，面板继续保留并显示“当前局面暂无注释”，按钮仍可关闭。
- 原先随 `currentId` 变化强制收起的逻辑已移除；不影响注释编辑、棋盘标注和保存。
- 验证：`npx tsc --noEmit` 通过；`npm run build` 通过，`dist` 约 4.87MB；390×844 浏览器回归打开注释→下一手保持→无注释提示→再次点击关闭，页面错误 0。

### 第 70 轮｜回收站（2026-08-29）

- 棋谱库新增回收站入口，覆盖普通棋谱、大型棋谱和用户导入题集；内置题库不可删除。
- 删除改为软删除：普通对象保留在 `banbu-recycle-bin-v1`，大型棋谱在 IndexedDB 独立回收站 stores 内转移，不把大谱整体写入 localStorage。
- 回收站支持恢复、彻底删除、清空，并保留原文件夹归属；回收站数据纳入一键备份与恢复。
- 新增 `src/recycle-bin.ts`、`src/recycle-bin.test.ts`；大型存储版本升级到 8，新增回收站 stores 与对应迁移读写。
- 验证：全量测试 25 个测试文件，194 项通过、1 项跳过；`npx tsc --noEmit` 通过；`npm run build` 通过，`dist` 约 4.86MB；390×844 网页回归覆盖普通棋谱删除/恢复、用户题集删除/彻底删除，页面错误 0。

### 并发任务 6｜分享图片导出（2026-08-29）

- 已完成当前局面的 1200×1400 高清 PNG 分享卡片，支持手数、坐标、棋盘标注、水印四项开关，并保持当前旋转/镜像方向。
- 已接入“导出与分享”面板；使用 `viewDocument`，未提交草稿也能按当前画面导出。
- 分享路径：Web Share 文件分享 → Capacitor Filesystem + Share 原生系统分享 → 不支持时自动下载 PNG。
- 新增 `src/board-image-export.ts`、`src/share-file.ts`、`src/board-image-export.test.ts`、`qa/share-image-export.mjs`；详细边界和验收见 `054-GPT分享图片导出方案与实现.md`。
- 验证：`npm test` 157/157 通过；`npm run build` 通过，`dist` 4.62MB；390×844 浏览器 QA 三条路径通过；实际 PNG 1200×1400、121835 bytes。
- `npx cap sync android` 已同步 `@capacitor/filesystem@8.1.3` 与 `@capacitor/share@8.0.1`。Android 本地编译仍受无效 `JAVA_HOME=E:\java\`、缺少 JDK 21 限制。

### 第 73 轮｜批量编辑、无障碍字号与 i18n 框架（2026-08-29）

- 棋谱库新增普通棋谱批量编辑模式：支持全选当前搜索结果、清空选择、逐项勾选和退出批量编辑。
- 批量操作 sheet 支持批量替换节点注释/局面文字，以及批量导出 SGF；一次最多导出 20 份，避免移动设备瞬时占用过多内存。
- 大型 LIB、DP、DB 不进入批量改写链路，只保留原有单独打开/导出能力，避免误改原始数据库。
- 新增 `src/accessibility.ts`：字号支持正常、大字（115%）、特大字（130%），只放大界面文字与控件，不整体缩放棋盘；偏好保存在 `banbu-font-scale-v1`。
- 新增跳到主要内容链接、`main#main-content`、底部导航 `aria-current` 和批量选择状态 `aria-live`。
- 英文 i18n 入口按用户要求撤回，当前版本不显示语言切换，也不保留半成品英文 UI；后续需要国际化时再重新设计接入范围。
- 补齐可选增强设置面板的内联实现，消除既有未定义组件引用，保证构建链路完整。
- 英文入口本轮已撤回：不再保留语言切换卡片、英文文案或 `src/i18n.ts`；字号设置保留为中文设置页中的默认折叠分组。
- 验证：`npm test -- --run` 28 个测试文件，203 项通过、1 项跳过；`npm run build` 通过，dist 约 4.88MB；`git diff --check` 通过（仅既有 CRLF 提示）；Playwright 390×844 已验证字号切换、批量入口和选择模式。

### 第 77 轮｜DP/DB 编辑副本回退保留原谱标签（2026-08-30）

- 修复真实 `D:\五子棋\其他\九天指南v4-2.db` 在分支终点点击“在此处继续编辑”后回退，来路原谱标签丢失并被灰点覆盖的问题。
- `DpViewSession.projection()` 现在将父查询分支边上的标签带到已选路径节点，编辑副本复制后仍保留 `boardText`、原谱标签标识和颜色。
- 新增/扩展 `qa/dp-a-repro.mjs`，覆盖 `H8 → G9 → J6 → G10 → 编辑副本 → 连续回退`，断言 `G10=A`、`J6=彗`、`G9=斜`，并确认灰点为 0；同时验证终点续谱和旧分支位置点击仍可创建新落子。
- 真实文件在 5181 源码预览和 5182 生产预览均通过，页面错误 0；分支导航专项通过；23 个原谱标签视觉检查通过，灰点 0、重复坐标 0。
- 本轮验证：29 个测试文件、208 项通过、1 项跳过；`npm run build` 通过，dist 4.91MB。未重置或覆盖其他并发任务改动，未重新打包 APK。
- 详见 [077-GPT修复v4-2编辑副本回退原谱标签丢失.md](./077-GPT修复v4-2编辑副本回退原谱标签丢失.md)。

### 第 78 轮｜并发任务收口、全量回归与集成验收（2026-08-30）

- 收口并集成已完成的并发功能：AI 强度/人机对局时长、音效与动效设置、VFX 轻量交互、导入进度与最近导入、回收站、备份恢复、批量编辑与字号、反馈入口、棋谱树、图片导出、ErrorBoundary/诊断、RenLib 开发资源、DP/DB 编辑副本修复、题库错题本，以及主题/棋盘/棋子外观和 Logo 候选资产。
- 无文档但已从会话恢复并通过黑盒验收的“快捷中心”：支持思考后直接落子、思考结果入口、主题/棋盘/棋子即时切换；实测空棋谱自动落子成功，焦点恢复和页面错误检查通过。
- 最终验证：`npm test -- --run` 为 29 个测试文件通过、1 个跳过，208 项通过、1 项跳过；`npx tsc --noEmit` 通过；`npm run build` 通过，`dist` 约 4.91MB；`git diff --check` 通过（仅 LF/CRLF 提示）。
- `npm run qa:browser` 全部通过：高级导入、百万节点分块顺序、草稿保护、最近导入、AI 规则与人机对局、编辑命令栏、SGF/JSON 导出、移动端、多视口、ErrorBoundary、RenLib 开发资源；页面错误 0、无横向溢出。
- 真实文件回归仍通过：`D:\五子棋\其他\九天指南v4-2.db` 的 DP/DB 编辑副本回退、原谱标签保留、分支导航和续谱；23 个原谱标签、23 个变化点、灰点 0、重复坐标 0。
- 本轮未纳入提交的仅有生成型临时证据：`tmp-settings.png`、`qa/tmp-ai-benchmark-report.json` 及 `qa/` 下的临时截图；它们没有被删除或覆盖，仍保留在本地工作区。
- 当前最新网页版：`http://127.0.0.1:5181/`；本轮未重新打包 APK，Android 构建环境仍是 Java 8，不能据此宣称本轮 APK 已重新验证。

### 第 79 轮｜发布前全功能回归（2026-08-30）

- 用户当前最新网页 `http://127.0.0.1:5181/` 完成最终 `npm run qa:full`：25 个源码测试文件、183 项确定性用例通过；11 个正式浏览器门禁全部通过；TypeScript 构建、Vite 生产构建和 20MB 体积门禁通过，`dist` 约 4.91MB。
- 补充专项全部通过：音效/静音与音量持久化、动效设置、导入阶段进度、历史点标注、原谱当前棋子标记、PNG 分享、AI 计时、导出/标注/本地 Rapfi；无页面错误。
- 真实数据全部通过：`D:\五子棋\其他\九天指南v4-2.db` 共 5514 个局面，DP/DB 查询路径、A 分支检索、浏览器分支导航、编辑副本回退标签和续谱通过；8 个内置题库 JSON 共解析 433 道题，仅按原数据规则跳过不合法题目，无警告。
- 网页 RenLib 大文件动态会话通过：`雨.lib` 4.44MB 导入约 2.5 秒，`松月.lib` 131.77MB 导入约 8.4 秒；两者导航 p95 约 44ms、无页面错误。原始 `.lib` 刷新后不会自动恢复文件选择，这是浏览器文件输入权限边界，不作为已恢复能力宣称。
- 本轮仅修正了 5 个陈旧专项脚本假设：音效默认关闭前置、动态计时首秒断言、设置折叠入口、当前 RenLib/DP 架构状态和大谱网页会话状态；未修改产品源码。视觉回归首次出现一次平板 254ms 长任务阈值边缘抖动，单独重跑和最终全量均通过。
- 未重新打包 APK；当前 Android 环境仍为 Java 8，因此本轮不宣称 Android 安装包已重新验证。未跟踪文件仍仅为既有截图、临时 AI 报告和测试生成证据，未删除或覆盖。

## 新对话恢复方式

详细历史、技术架构、真实验证边界和后续维护规约见 [051-GPT项目总览与后续维护基线.md](./051-GPT项目总览与后续维护基线.md)。

新建对话后只需要发送：

```text
读取 D:\Projects\五子棋\项目资料\半步五子棋\docs\模型沟通\当前项目状态.md，
然后在项目目录执行 git status 和必要的验证，不要读取旧聊天，直接从当前工作区继续。
```

恢复时的顺序：

1. 读取本文件。
2. 查看 `git status --short` 和相关源码 diff。
3. 根据用户最新要求继续；不把历史聊天当作唯一上下文。
4. 完成后同步更新本文件，并在回复中同时给出网页版地址、网页版构建产物和 APK 路径（如果本轮涉及它们）。

## 关于 Codex 会话异常

目前观察到的是 Codex 任务历史投影异常，不是本项目代码导致：

- 多个新旧任务在分叉时出现 `thread-store internal error`，典型信息是 `expected ordinal ..., got ...`。
- `111` 新对话即使建立后也会复现，说明不是单纯因为旧聊天太长，也不是只绑定项目即可解决。
- 重新打开后只看到最初交接消息，说明后续上下文没有被当前任务历史投影完整恢复。

本文件是项目级的抗丢上下文方案：即使聊天重启、重开或无法分叉，也能从当前工作区恢复。它不能修复 Codex 服务端的历史投影或分叉接口；接口问题需要平台侧修复。在平台修复前，使用“新对话 + 本文件”可以保持轻量，不需要回到旧大对话。
