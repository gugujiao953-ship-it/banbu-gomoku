# 104 · dsh 接手补充：做题「分析」卡死修复线的最新状态（2026-09-10 深夜）

> 给 dsh：你汇报里看的中间态（未提交的三处改动）**已经全部落地并提交**，且其中一处方案
> 已被用户后续拍板**推翻重做**。先读本文对账，再决定做什么；不要重复实现，不要动
> `scripts/verify-ai-rules.mjs`（归属存疑，见下）。
> 项目根：`D:\Projects\五子棋2`（git main）。上游登记：`docs/1.1.8-版本报告.md` T40 节。

---

## 一、30 秒结论

做题「分析」点下去几十秒不落子（用户 09-10 反馈）这条线**已完成、已提交、门禁已绿**。
dsh 汇报的三处改动现状：

| dsh 看到的改动 | 最终状态 |
|---|---|
| `src/App.tsx`：做题强制轻量引擎 | **已推翻**（`43223e5`）：用户拍板「不要掐算力，引擎跟着设置走」——做题分析恢复跟随 `aiEngineChoice`（选强力就用强力）；预算改 10s |
| `public/rapfi/rapfi-worker.js`：provenWin 兜底 | **保留**（`38741e9` 根修）：置 `stopRequested` + 800ms 兜底 timer 强制 finish |
| `scripts/verify-ai-rules.mjs`：门禁适配新 UI | **未动、未提交**（mtime 09-08，早于本线 4 天，归属存疑，按纪律不动） |

## 二、完整提交链（git show 定位）

- `38741e9` fix：provenWin 永挂根修 + （当时的）做题强制轻量——**只看 worker 部分**，App 部分已被下一提交改写
- `43223e5` feat：预算 3s→**10s**、**撤销做题强制轻量**（引擎跟随设置）、**新增强力引擎启动预热**（worker `warmup` 消息 + App useEffect）
- `b44a393` docs：登记 T40 复选 4

三者一起看，最终行为是：

1. **worker provenWin 不再永挂**：`EVAL +M`/胜率≥0.999 命中时置 `stopRequested=true`（裸坐标也能收尾）+ 保留 800ms 兜底 timer 用当前 PV0 强制 finish。full 构建对 YXSTOP 的响应不带 `MESSAGE Bestline` 前缀，旧代码只等该前缀=引擎已停 worker 永等（复现 87s+ 零消息）。
2. **做题分析引擎跟随设置**：`startThink` 的 `analysisPackUrl = aiEngineChoice === "strong" ? enginePackUrlRef.current : null`（无 mode 特判）；「强力引擎包未就绪」toast 恢复全场景。
3. **预算 10s**：`PUZZLE_HINT_THINK_TIME_MS = 10000`，打谱/做题提示统一；按钮文案「≤10 秒」；杀棋即落不变（`stopOnProvenWin + finishOnBareMove` 有界轮）。
4. **强力引擎启动预热**（根治现场冷加载）：worker 新增 `warmup` 消息（只钉 enginePreference/dataUrlOverride + load()，不分析）；App 在 `aiEngineChoice==="strong" && enginePackReady` 时提前创建常驻 worker 并 warmup（slot key=`strong:<blob>`，startThink 按 key 复用）。加载失败（fullBlocked）回落轻量；低内存设备由 worker 内 `chooseVariant` 的 deviceMemory 门槛自动降级。

## 三、dsh 汇报逐条对账

- ✅ tsc 0 错 / vitest 448 项（dsh 跑出 448/448，我们后来全量 448 项 1 失败=VCF 墙钟 flake，独占复跑绿）——结论不变：VCF 两用例负载敏感，别当回归。
- ✅ provenWin 复用 stopTimer 槽位被 clearRequestTimers 清理、stopRequested 与 T29/scheduleStop 同语义——dsh 的逻辑审查正确，最终实现保留了该语义（stopRequested=true 即复用该路径）。
- ❓ **「做题永远走轻量」的取舍——已拍板推翻**：用户明确「不要掐算力，最多十秒可以，只要算到答案马上落子；引擎跟着设置走」。所以没有"做题棋力上限钉在轻量"的代价了——做题用强力时就是强力棋力，加载问题由预热解决。
- ❓ 端到端验证：**已做**。预热后做题+强力 **531ms 落子**（杀棋即落）；打谱+强力+提示 12.3s（含冷加载，不再永挂）；做题+轻量 500ms。
- ❓ 门禁回归：**已做**。`ui-puzzle-layout-check` 17 断言、`ui-hint-selfplay-check` S1-S7、`ui-hint-stop-check` H1-H3 全绿。
- ❓ 临时探针：dsh 看到的 5 个 `.tmp-*` 是当时诊断用的，**已全部删除**。现在 untracked 只剩更早遗留（见下）。
- ✅ 未登记：**已登记**（`b44a393`，T40 节「同日复选 4」）。

## 四、工作区现状（动手前 git status）

- 唯一未提交改动：`scripts/verify-ai-rules.mjs`（M）——**不是本线产物**（mtime 09-08，内容适配 T5 后的新 UI 与 T29 停止语义）。按仓库纪律不提交他人未提交文件；若 dsh 想认领需先与用户/其他会话确认归属，否则保持不动。
- untracked 遗留（都不建议碰，除非用户点名）：
  - `.tmp-mine.patch` / `.tmp-reduce.patch` / `.tmp-styles-mine.patch`（09-08，疑似早期样式对比残留）
  - `design-lab/`（09-04 设计实验，暂不入库，.gitignore 外）
  - `src/tmp-forbidden-check.test.ts`（09-06 临时测试，可删但先确认无人用）

## 五、真正还剩下的待办（按优先级）

1. **真机端到端**（最值得做）：Redmi K20 WebView 上验证——①预热后做题点分析秒落；②**首启未预热就点分析**的冷加载体验（预计 10-30s，看是否可接受；若不可接受再议预热时机，如切到做题 tab 时预热）。
2. **verify-ai-rules.mjs 归属**：向用户确认是否认领/删除/保持。
3. **顺手遗留**（与本题无关但已登记）：`qa/mobile-visual.mjs` 仍含 T36 前过时定位（dock-tabs「应战」断言，跑必挂，T40 已知遗留）；手册 `s14f1.jpg` 是旧截图（T39 遗留，需截图管线重拍）。
4. 版本报告 T40 节可补一条「真机验收结果」收尾。

## 六、重跑验证命令（想复核时）

```bash
cd D:/Projects/五子棋2
npx tsc --noEmit
BANBU_URL=http://localhost:5193 node scripts/ui-puzzle-layout-check.mjs
BANBU_URL=http://localhost:5193 node scripts/ui-hint-selfplay-check.mjs
BANBU_URL=http://localhost:5193 node scripts/ui-hint-stop-check.mjs
# 做题+强力预热实测（需先下载强力包，约 40MB）：
# 设置页下载强力引擎包 → 等 ~6s 预热 → 做题选三手胜题点分析，应 ≤1.5s 落子
```
