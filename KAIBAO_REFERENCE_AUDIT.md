# 开宝五子棋陪练参考审计

## 样本与范围

- 样本：`base.apk(3).1`
- SHA-256：`E393EF9FC64F63F4B5D3A20B15AB9536D97616EB7034498FB2DDFA501D9CE3C6`
- 包名：`com.js.renjupartner`
- 版本：1.5.1（versionCode 11）
- Android：minSdk 26、targetSdk 31、竖屏 Compose 界面
- 原生库：`arm64-v8a/libRenjuAi.so`，SHA-256 `443678dec22f6290a0160b33cd510e953835db53d6c45f4b06ed77efabcb3ad9`

本审计通过 APK 解包、JADX/Androguard 反编译、ELF 符号与 ARM64 指令核对，以及官方 SlowRenju 仓库源码比对完成。反编译产物仅作为功能和兼容性研究证据，不随产品发布。

## 最值得迁移的交互

1. 题目初始盘面与本轮尝试分离。初始棋子不进入 `moveList`，因此悔棋和重来不会破坏题面。
2. 玩家落子后立即在后台启动陪练；思考期间禁止重复落子和切题。
3. 悔棋先停止 AI，再回退玩家和 AI 的一整个回合。
4. 重来先停止 AI，只移除本轮新增着，恢复题目初始快照。
5. 题集、题号和棋盘位于同一操作流中；无落子或已结束时可左右切题。
6. 每题使用状态位保存已解和收藏，题集显示总数与完成数。

当前项目采用显式“打谱 / 做题”双面分段按钮、题面快照、成对悔棋、题内重启、可终止 Worker、棋盘页前后切题和棋谱库中的题集文件夹入口。

## 题库格式

APK 含 8 个 JSON 题集，原始记录 444 条，其中 11 条为空占位，实际 433 题。每题是字符串数组：

```json
["★", "J10,1", "G9,2"]
```

- `A`–`O` 为列，`1`–`15` 为行；尾码 `1` 为黑、`2` 为白。
- 不含逗号的 `★` / `★★` 是题目标记。
- 所有坐标均是初始摆题棋子，不是答案变化线。
- 先手按初始棋子总数判断：偶数黑先、奇数白先。
- 题目没有内置答案树，用户落子后由 AI 实时对抗。

网页版已实现这一 JSON 的兼容导入、空项跳过、星级提取、坐标/颜色/重复点校验和用户来源标记。

## 引擎结构

`libRenjuAi.so` 与 SlowRenju 的棋形表、评价参数、VCF 数据和候选邻域表逐字节吻合，确认属于其改版/派生。核心结构为：

- 迭代加深 PVS / Negamax Alpha-Beta；
- Zobrist 两槽置换表与最佳着排序；
- 四方向增量棋形缓存；
- 立即胜、唯一防、VCF、普通候选的分阶段搜索；
- 局部候选、攻守联合评分和 Top-K；
- 递归禁手验证；
- 节点入口和 VCF 递归入口的停止标志检查。

APK JNI 只导出 `findMove` 与 `stopThinking`，固定约 200 万节点，调用 `rootsearch(6,60,1,1)`。原版使用 `volatile` 全局标志停止，线程安全和停止延迟并不理想；源码置换表约 256 MiB，APK `.bss` 约 192 MiB，也不适合原样照搬到普通手机。

当前网页版采用独立实现的迭代加深 Alpha-Beta、小型置换表、攻守候选排序和 Web Worker。停止通过终止 Worker 完成，旧 Worker 的结果由实例身份隔离，不能回写已经重启或切换的题面。它保留了算法分层思想，但没有复制 SlowRenju 的源码、棋形表、权重或独特常量。

## 许可证结论

- SlowRenju 代码为 GPLv3。直接复制代码、表、权重、二进制或通过 JNI 随应用分发，会带来完整对应源码、构建脚本、修改声明、版权与 GPL 许可等义务。
- APK 的原生库已经修改，公开仓库不是该二进制的精确对应源码，不能用公开仓库冒充 APK 的 Corresponding Source。
- APK 题库 JSON 没有发现单独的许可证、作者或来源声明；GPL 不会自动覆盖这些题目内容。

当前本地构建已按用户对其所提供 APK 的明确要求接入 433 道有效题，并在题集信息中标注来源为“开宝五子棋 1.5.1 · 用户提供 APK · 本地使用”。若未来公开发布或商业分发，仍应先确认题库权利范围，并为每个题集保存来源、作者、许可证和署名。

## 后续引擎路线

若产品选择 GPLv3 开源，可以重新接入可核验的 SlowRenju 对应源码，同时重构全局状态、原子取消、内存预算和 Android 构建链。若要保持其他许可，则继续 clean-room 路线：补齐精确连珠禁手、VCF/VCT 威胁空间搜索、Zobrist TT、增量棋形与可配置 16–64 MiB 内存预算。
